## 2026-04-22 - US-054
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-039` — first run: 14 generated, 6 missing/failed
  - No fixes needed; 6 permanent needs-oracle entries this batch
  - Generated: 0762-0763, 0767-0769, 0771-0772, 0774-0780

- **Files changed:**
  - Added: 14 stubs (0762-0780 range, excluding missing) in leetcode-playground/
  - Added: 14 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/needs-oracle.json

- **Learnings:**
  - `0761-special-binary-string`, `0764-largest-plus-sign`, `0765-couples-holding-hands`, `0766-toeplitz-matrix`: no oracle in solutions-bank, logged as oracleTier=missing.
  - `0770-basic-calculator-iv`: oracle could not generate valid test cases (complex symbolic expression evaluator with custom Expr class); logged as "no valid test cases generated".
  - `0773-sliding-puzzle`: oracle could not generate valid test cases for board serialization; logged as "no valid test cases generated".
  - `0776-split-bst` is a pointer-tree problem and was handled correctly by the tree category in generate_tests.py.
---

## 2026-04-22 - US-053
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-038` — first run: 15 generated, 5 missing/failed
  - Fixed all 5 failures with slug overrides and re-ran: 5 more generated; total 20/20
  - 0 permanent needs-oracle entries this batch
  - Generated: 0741-0760

- **Files changed:**
  - Added: 20 stubs (0741-0760) in leetcode-playground/
  - Added: 20 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (added slug overrides for 5 problems)
  - Modified: scripts/ralph/needs-oracle.json

- **Learnings:**
  - `network-delay-time` is a 3-param `(times: number[][], n: number, k: number)` where times[i]=[u,v,w] (3-element edge arrays). The generic `(number[][], number)` 2-param handler doesn't match 3 params → falls to fallback emitting 1D array as `times`. Fix: add slug override with valid directed weighted edges.
  - `shortest-completing-word(licensePlate: string, words: string[])` is `(string, string[])` — no generic handler for this combo exists, falls to fallback. Fix: add slug override with license plate strings and word arrays.
  - `pyramid-transition-matrix(bottom: string, allowed: string[])` same `(string, string[])` pattern — no generic handler. Fix: add slug override with valid bottom strings and 3-char allowed patterns.
  - `bold-words-in-string(words: string[], s: string)` is `(string[], string)` — no generic handler for this combo. The `string[]` single-param handler doesn't fire because len(types)==2. Fix: add slug override.
  - `WordFilter` (prefix-and-suffix-search) has method `f(pref, suff)`. Method name `f` matches nothing in design-class handlers → generic fallback fires with `[fn_name]` (no constructor args) and `[f, 1, 1]` (integer args for string params) → TypeError on `pref.length`. Fix: add slug-based override that passes `(string[] words)` to constructor and string pairs to `f()`.
---

## 2026-04-22 - US-052
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-037` — first run: 13 generated, 7 missing/failed
  - Fixed 4 failures and re-ran: 4 more generated; total 17/20
  - 3 permanent needs-oracle: 0716 (PriorityQueue), 0729 (no oracle), 0732 (no oracle)
  - Generated: 0707, 0723-0728, 0730-0731, 0733-0740 (minus missing)

- **Files changed:**
  - Added: 17 stubs (0707-0740 range) in leetcode-playground/
  - Added: 17 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (ListNode[] render fix, flood-fill slug override, sentence-similarity slug override)
  - Modified: scripts/ralph/needs-oracle.json

- **Learnings:**
  - `ListNode[]` return type was matched by the generic `'ListNode' in return_type` check in `render_it_block`, generating `expect(listToArray(result)).toEqual(...)` — but `listToArray` expects a single ListNode, not an array. `listToArray([ListNode1, ListNode2])` returns `[undefined]` (array has no .val, loop exits after 1 step). Fix: add dedicated `ListNode[]` check BEFORE the generic check, using `result.map(n => listToArray(n))`.
  - `(number[][], number, number, number)` 4-param pattern (flood-fill: image + sr + sc + newColor) has no generic handler; falls to fallback which emits single-array inputs missing the 3 int params. Need slug override.
  - `(string[], string[], string[][])` 3-param pattern (sentence-similarity: sentence1 + sentence2 + similarPairs) has no generic handler; falls to fallback. Need slug override with valid sentence pairs and similarPairs matrix.
---

## 2026-04-22 - US-051
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-036` — first run: 16 generated, 4 missing/failed
  - 4 permanent needs-oracle: 0702 (ArrayReader mock API), 0703 (PriorityQueue platform class), 0708 (circular linked list serialization loop), 0710 (non-deterministic pick())
  - Generated: 0701, 0704-0706, 0709, 0711-0715, 0717-0722

- **Files changed:**
  - Added: 16 stubs (0701-0722 range) in leetcode-playground/
  - Added: 16 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/needs-oracle.json

- **Learnings:**
  - `ArrayReader` API (0702) is a mock interface like read4; oracle uses `reader.get(i)` not direct array access — permanent needs-oracle
  - Circular linked list (0708) causes infinite recursion in serialization just like doubly-linked-list (0426) — permanent needs-oracle for any circular pointer structure
  - `RandomPickWithBlacklist.pick()` (0710) is non-deterministic — permanent needs-oracle
---

## 2026-04-22 - US-050
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-035` — first run: 16 generated, 4 missing/failed
  - Fixed 0684 (redundant-connection): generic `number[][]` handler emits `[[]]` and `[[1]]` (invalid edges); added slug override with valid `[ai, bi]` edge pairs forming trees with one redundant edge
  - Fixed 0688 (knight-probability-in-chessboard): 4-param `(n, k, row, column)` has no generic 4-int handler; added slug override with valid board+move+position inputs
  - Fixed 0690 (employee-importance): `GetImportance` is PascalCase → detected as design-class; added to PLAIN_FN_SLUGS and added slug override with Employee `{id, importance, subordinates}` objects; also fixed `get_input_category` and `get_assertion_template` to skip PascalCase check for PLAIN_FN_SLUGS entries
  - Fixed 0699 (falling-squares): generic `number[][]` handler emits `[[1]]` (invalid 1-element position); added slug override with valid `[left, sideLength]` 2-element pairs
  - Re-ran: all 20/20 generated; 0 permanent no-oracle entries this batch
  - Generated: 0681-0700

- **Files changed:**
  - Added: 20 stubs (0681-0700) in leetcode-playground/
  - Added: 20 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (PLAIN_FN_SLUGS extended; PascalCase bypass for PLAIN_FN_SLUGS in get_input_category + get_assertion_template; slug overrides for 0684/0688/0690/0699)
  - Modified: scripts/ralph/needs-oracle.json

- **Learnings:**
  - PascalCase function name detection (`fn_name[0].isupper()`) must be bypassed for PLAIN_FN_SLUGS entries in both `get_input_category` and `get_assertion_template`; just setting `class_heavy = False` is insufficient when the PascalCase check runs independently
  - `employee-importance` oracle uses `GetImportance(employees[], id)` — PascalCase but plain function; Employee objects are plain JSON `{id, importance, subordinates}` and pass through `convertArg` unchanged (ptype=undefined path)
  - `redundant-connection` edges must be `[ai, bi]` 1-indexed pairs forming a tree + 1 redundant edge; generic `number[][]` catalog includes `[[]]` and `[[1]]` (invalid formats) which crash the oracle
  - `falling-squares` positions must be `[left, sideLength]` 2-element arrays with sideLength > 0; generic `number[][]` catalog emits 1-element sub-arrays causing NaN results
  - 4-param integer functions (n, k, row, column) have no generic handler; always need slug override
---

## 2026-04-22 - US-049
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-034` — first run: 15 generated, 5 missing/failed
  - Fixed 0676 (MagicDictionary): generic Trie handler doesn't handle `buildDict(string[])` constructor; added dedicated handler
  - Fixed 0677 (MapSum): generic fallback passes integer 1 for string keys; added `insert+sum` handler with proper (string, number) pairs
  - Fixed 0642 (AutocompleteSystem): Trie handler fires (oracle has `insert`+`search` helpers) but constructor needs `(sentences[], times[])`; generic fallback called constructor with no args → `sentences.length` TypeError. Added slug-based AutocompleteSystem handler before Trie handler
  - Re-ran: 3 more generated; total 18/20 generated; 2 permanent no-oracle (0675, 0679)
  - Generated: 0642, 0662-0674, 0676-0678, 0680

- **Files changed:**
  - Added: 18 stubs (0642-0680 range) in leetcode-playground/
  - Added: 18 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (AutocompleteSystem handler, MagicDictionary handler, MapSum handler)
  - Modified: scripts/ralph/needs-oracle.json

- **Learnings:**
  - `MagicDictionary.buildDict(string[])` is not `insert` or `addWord`; the Trie handler won't fire, and generic fallback passes integer 1 for the array param → TypeError. Add dedicated `buildDict` method-name handler.
  - `MapSum.insert(string, number)` has `insert` in method_names but NOT `search`; the Trie handler requires `insert+search` so it won't fire, but the generic fallback passes integer 1 for string keys. Guard the MapSum handler as `insert+sum+not search`.
  - `AutocompleteSystem` oracle has internal `insert(sentence, count)` and `search(node, prefix, candidates)` helper methods on the prototype; `parse_class_methods` picks them up, so `method_names = ['input', 'insert', 'search']`. The Trie handler fires and generates sequences with `[fn_name]` (no constructor args), crashing on `sentences.length`. Fix: add slug-based check for `autocomplete` in slug OR detect by `input+insert+search` combo — placed BEFORE the Trie handler.
---

## 2026-04-22 - US-048
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-033` — first run: 16 generated, 4 missing/failed
  - Fixed 4 failures and re-ran: 4 more generated; total 20/20
  - Generated: 0635, 0641, 0644-0661 (excluding SQL/no-oracle: none this batch)

- **Files changed:**
  - Added: 20 stubs (0635-0661 range) in leetcode-playground/
  - Added: 20 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (LogSystem handler, 4-keys-keyboard override, replace-words override, boolean return_type fix for any-valid-pair-summing)
  - Modified: scripts/ralph/needs-oracle.json (0648 entry removed after fix; 0651/0653/0635 fixed)

- **Learnings:**
  - `two-sum-iv-input-is-a-bst` returns `boolean`, not an index pair; `get_assertion_template` was matching "two-sum" in slug and returning `any-valid-pair-summing` which calls `.length` on boolean → TypeError. Fix: add `and return_type != 'boolean'` guard to the two-sum slug check.
  - `4-keys-keyboard` (maxA): `dp = new Array(n + 1).fill(0)` with generic n=1534236469 → OOM; need slug override with small n values.
  - `replace-words` has `(string[], string)` param combo with `first = 'string[]'` and `len(types) == 2`; no generic handler exists for this two-param pattern (only `len(types) == 1` is handled for `string[]`); falls to numeric fallback → oracle crashes. Need slug override with valid `(dict, sentence)` pairs where dict roots appear as prefixes in sentence words.
  - `design-log-storage-system` (LogSystem): `put(id, timestamp)` and `retrieve(start, end, granularity)` — generic fallback passes `[1]*len(params)` so retrieve gets `start=1` (number), calling `1.slice(0, precision)` → "start.slice is not a function". Need custom LogSystem handler with proper ISO-format timestamp strings ("YYYY:MM:DD:HH:MM:SS") and granularity keywords ("Year"/"Month"/"Day"/"Hour"/"Minute"/"Second"). Handler detects by checking `'put' in method_names and 'retrieve' in method_names`.
---

## 2026-04-22 - US-047
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-032` — first run: 11 generated, 9 missing/failed
  - Fixed 5 missing: added slug overrides for task-scheduler, exclusive-time-of-functions, shopping-offers, solve-the-equation, maximum-average-subarray-i
  - Fixed 0631 (design-excel-sum-formula): added Excel handler in `_build_design_inputs` with `(number, char)` constructor + set/get/sum ops
  - Fixed 0630 (course-schedule-iii): uses `PriorityQueue` platform class — added generic check in `generate_problem` to skip any oracle with `new PriorityQueue(` pattern
  - Re-ran generator: 6 more generated; total 17/20 generated; 3 permanent missing (2 SQL + 0630 PriorityQueue)
  - Generated: 0621-0625, 0628-0629, 0631-0634, 0636-0640, 0643

- **Files changed:**
  - Added: 17 stubs (0621-0643 range) in leetcode-playground/
  - Added: 17 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (PriorityQueue detection, 5 slug overrides, Excel design handler)
  - Modified: scripts/ralph/needs-oracle.json (0630 added)

- **Learnings:**
  - `(character[], number)` param combo (task-scheduler) is not matched by the `(number[], number)` handler; need slug override with char array inputs
  - `(number, string[])` param combo (exclusive-time-of-functions) with domain-specific string format (logs "fnId:start|end:ts") needs slug override; generic string[] catalog passes arbitrary strings
  - `(number[], number[][], number[])` 3-param combo (shopping-offers) has no generic handler; need slug override
  - Single-string equation format (solve-the-equation) needs slug override; generic single-string catalog passes arbitrary strings
  - `(number[], number)` generic handler (kth-largest pattern) passes k values that exceed array length — causes issues for findMaxAverage; need slug override with valid k ≤ len(nums)
  - Generic PriorityQueue detection: added `re.search(r'new\s+(MaxPriorityQueue|MinPriorityQueue|PriorityQueue)\s*\(', oracle_source)` check immediately after loading oracle source — skips before any further processing. This prevents false successes and is more robust than slug-by-slug tracking.
  - Excel design class uses `(number, character)` constructor args where width is a char ('C', 'E'); need custom handler with string column letters in all method calls
---

## 2026-04-22 - US-046
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-031` — 6 generated, 14 missing/failed
  - 13 SQL/no-oracle skips: 0601, 0602, 0603, 0607, 0608, 0610, 0612, 0613, 0614, 0615, 0618, 0619, 0620
  - 1 failed validation: 0616 (add-bold-tag-in-string) — no valid test cases generated, logged to needs-oracle.json
  - Generated: 0604, 0605, 0606, 0609, 0611, 0617

- **Files changed:**
  - Added: 6 stubs (0604-0617 range) in leetcode-playground/
  - Added: 6 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/needs-oracle.json (0616 added)

- **Learnings:**
  - `add-bold-tag-in-string` (0616) has `(string, string[])` param combo; generic catalog generated no valid cases (validation failed all 3 attempts); needs slug override to produce proper (s, words) pairs

---

## 2026-04-22 - US-045
- **What was implemented:**
  - Ran `python3 scripts/ralph/expand_prd.py` — seeded 10 batch stories (batch-031 through batch-040) + 1 Expand story
  - expandedUpTo: 30 → 40 of 150

- **Files changed:**
  - Modified: scripts/ralph/prd.yaml (US-045 passes: true + US-046..US-056 appended)
  - Modified: scripts/ralph/manifest.json (expandedUpTo: 40)

- **Learnings:**
  - ruamel.yaml not installed on Python 3.13 env; install via `curl -sS https://bootstrap.pypa.io/get-pip.py | python3 - --break-system-packages` then `python3 -m pip install ruamel.yaml --break-system-packages`
  - expand_prd.py automatically marks the active Expand story passes: true before writing files

---

## 2026-04-22 - US-044
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-030` — first run: 6 generated, 14 failed
  - Fixed 8 failures with slug overrides and new N-ary tree support; re-run: 8 more generated (14 total)
  - 6 SQL problems (0584, 0585, 0586, 0595, 0596, 0597) remain as no-oracle skips (expected)
  - Generated: 0581-0583, 0587-0594, 0598-0600 (14 stubs + tests total)

- **Files changed:**
  - Added: 14 stubs (0581-0583, 0587-0594, 0598-0600) in leetcode-playground/
  - Added: 14 tests in leetcode-playground/tests/
  - Modified: test_helpers.js (added `_Node` class + `arrayToNaryNode`)
  - Modified: oracle_run.mjs (added `_Node` global injection + `_Node` convertArg)
  - Modified: run_test.mjs (added `_Node` global injection)
  - Modified: generate_tests.py (pointer-nary-tree category, slug overrides, FileSystem handler)
  - Modified: scripts/ralph/needs-oracle.json

- **Learnings:**
  - N-ary tree problems use `_Node` (not `TreeNode`); requires new `pointer-nary-tree` category in generate_tests.py, `_Node` global injection in oracle_run.mjs + run_test.mjs, and `arrayToNaryNode` converter in test_helpers.js
  - N-ary tree LeetCode format: `[root, null, child1, child2, ..., null, grandchild1, ...]` — null is a separator between sibling groups (one null per parent node)
  - `range-addition` slug check (`'range-addition' in slug`) matches both `range-addition` and `range-addition-ii`; always check for `-ii` variant first when the param signature differs
  - `kill-process(pid[], ppid[], kill)` is a 3-param (number[], number[], number) combo with no generic handler; needs slug override
  - `fraction-addition-and-subtraction` takes a single `string expression` but the oracle expects fraction format like `-1/2+1/2`; generic string catalog passes arbitrary strings
  - `valid-square` takes 4 separate `number[]` point args — same 4-param pattern as 4sum-ii; needs slug override
  - `minimum-index-sum-of-two-lists` takes two `string[]` arrays that must have overlapping elements; generic two-string-array catalog generates disjoint arrays
  - FileSystem design class (`ls+mkdir+addContentToFile+readContentFromFile`) needs a custom handler; generic fallback passes integer `1` for path strings causing `path.split is not a function`

---

## 2026-04-22 - US-043
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-029` — 9 generated (0561-0567, 0572, 0575), 0 skipped, 11 missing/failed
  - 8 SQL/no-oracle skips: 0569, 0570, 0571, 0574, 0577, 0578, 0579, 0580
  - 3 additional missing/failed: 0568 (maximum-vacation-days), 0573 (squirrel-simulation), 0576 (out-of-boundary-paths)

- **Files changed:**
  - Added: 9 stubs (0561-0567, 0572, 0575) in leetcode-playground/
  - Added: 9 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/needs-oracle.json

- **Learnings:**
  - No new slug overrides needed for this batch; all 9 generated successfully with existing generic handlers
  - SQL problems (0569-0571, 0574, 0577-0580) skip cleanly via no-oracle path — 8 out of 20 SQL is typical for this range

---

## 2026-04-22 - US-041
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-027` — 18 generated (0521-0540 except 0528/0534), 0 skipped, 2 missing/failed (0528, 0534)
  - Fixed 0524 (longest-word-in-dictionary-through-deleting): `(string s, string[] dictionary)` fell through to fallback. Added slug override with proper (s, dictionary) pairs.
  - Fixed 0526 (beautiful-arrangement): backtracking with large n (1534236469) → timeout. Added slug override with small n values (1-10).
  - Fixed 0529 (minesweeper): `(char[][] board, number[] click)` — no generic handler for char[][] + int[]. Added slug override with 'M'/'E'/'B' boards + click coordinates.
  - Fixed 0539 (minimum-time-difference): generic `string[]` catalog passes arbitrary strings; oracle expects "HH:MM" format. Added slug override with proper time strings.
  - 0528 (random-pick-with-weight): pickIndex() is non-deterministic → permanent needs-oracle.
  - 0534 (game-play-analysis-iii): SQL/database problem, no oracle → skipped.

- **Files changed:**
  - Added: 18 stubs (0521-0540 except 0528/0534) in leetcode-playground/
  - Added: 18 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (slug overrides for 0524/0526/0529/0539)
  - Modified: scripts/ralph/needs-oracle.json (0528 marked permanent)

- **Learnings:**
  - `(string, string[])` (string s + string[] dictionary) has no generic handler; falls through to fallback which passes wrong types. Add slug override for each such problem.
  - Backtracking functions (beautiful-arrangement) time out with large n same as fib; use slug override with small n values.
  - `(char[][], number[])` board+click combos need explicit slug overrides; no generic path handles char[][] inputs.
  - `string[]` generic catalog passes arbitrary strings; domain-specific string arrays (HH:MM, arithmetic expressions, etc.) need slug overrides.

---

## 2026-04-22 - US-040
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-026` — 15 generated (0501, 0503-0509, 0513-0518, 0520), 0 skipped, 5 missing/failed (0502, 0510, 0511, 0512, 0519)
  - Fixed 0509 (fibonacci-number): generic single-int catalog includes n=1534236469 → fib iterates ~1.5B times → timeout. Added slug override with small n values (0, 1, 2, 5, 10, 20, 30).
  - 0502 (ipo): uses `MinPriorityQueue`/`MaxPriorityQueue` (LeetCode platform classes, unavailable in Node.js) → permanent needs-oracle.
  - 0510 (inorder-successor-in-bst-ii): requires `_Node` with parent pointer — a different node type from `TreeNode`; not supported by test_helpers.js → permanent needs-oracle.
  - 0519 (random-flip-matrix): randomized/non-deterministic → permanent needs-oracle.
  - 0511, 0512: SQL database problems, oracleTier=missing → skipped.

- **Files changed:**
  - Added: 15 stubs (0501, 0503-0509, 0513-0518, 0520) in leetcode-playground/
  - Added: 15 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (fibonacci-number slug override)
  - Modified: scripts/ralph/needs-oracle.json (0502, 0510, 0519 marked permanent)

- **Learnings:**
  - `fib(n)` with n=1534236469 iterates through a loop of 1.5B steps → timeout; add slug override for fibonacci-number with small n values
  - 0502-ipo uses `MinPriorityQueue`/`MaxPriorityQueue` despite being tagged as a plain function (not design-class); these LeetCode platform classes are unavailable in Node.js → permanent needs-oracle
  - `_Node` with `.parent` pointer (inorder-successor-in-bst-ii) is a different structure from TreeNode; test_helpers.js only builds standard BSTs without parent pointers → permanent needs-oracle for parent-pointer problems

---

## 2026-04-22 - US-039
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-025` — 18 generated (0481-0500 except 0489/0497), 0 skipped, 2 permanent needs-oracle
  - Fixed 0481 (magical-string): generic single-int catalog includes n=1534236469 → timeout. Added slug override with small n values (1, 3, 5, 6, 8, 10, 20).
  - Fixed 0490 (the-maze): 3-param `(number[][], number[], number[])` fell through to generic fallback. Added slug override with specific maze+start+destination inputs.
  - Fixed 0492 (construct-the-rectangle): single-int generic catalog includes 0/-121 → oracle returns NaN. Added slug override with valid positive areas only.
  - Fixed 0496 (next-greater-element-i): two-array generic catalog passes arbitrary arrays where nums1 is not a subset of nums2. Added slug override with valid subset inputs.
  - Fixed 0499 (the-maze-iii): oracle source has no `@param` JSDoc → `parse_oracle` returns empty param_types → `_build_plain_json_inputs` exits early at `if not param_types: return []` before slug overrides. Fix: moved maze-iii slug override BEFORE the early return, and placed it in a new "pre-early-return slug overrides" block.
  - 2 permanent needs-oracle: 0489 (robot-room-cleaner uses Robot callback API), 0497 (random-point is non-deterministic).

- **Files changed:**
  - Added: 18 stubs (0481-0500 except 0489/0497) in leetcode-playground/
  - Added: 18 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (slug overrides for magical-string, the-maze, construct-the-rectangle, next-greater-element-i, the-maze-iii; pre-early-return block for no-JSDoc oracles)
  - Modified: scripts/ralph/needs-oracle.json (0489, 0497 permanent entries)

- **Learnings:**
  - `_build_plain_json_inputs` has an early `if not param_types: return []` guard that prevents slug overrides from firing for oracles without `@param` JSDoc. Any slug override needed for a no-JSDoc oracle must be placed BEFORE this early return in a "pre-early-return" block.
  - Oracle files without `@param` JSDoc (`parse_oracle` returns `[]`) still work correctly with oracle_run.mjs: `convertArg(arg, undefined)` returns `arg` unchanged (line 96: `if (!ptype) return arg`), so plain arrays pass through correctly.
  - `constructRectangle(area)` oracle returns NaN for area ≤ 0; generic single-int catalog includes negatives and zero → test cases with NaN get skipped. Use slug override with positive areas only.
  - `nextGreaterElement(nums1, nums2)` requires nums1 ⊆ nums2; generic two-array catalog generates arbitrary arrays violating this constraint.
  - `(number[][], number[], number[])` 3-param pattern (maze + 2 coordinate arrays) has no generic handler; always use slug override.

---

## 2026-04-22 - US-038
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-024` — 17 generated (0461-0479 except 0470), 0 skipped, 3 missing/failed (0470, 0478, 0480)
  - Fixed 0464 (can-i-win): generic two-integer catalog includes `[100, 1000]` → canIWin(100,1000) uses 2^100 bitmask states → timeout. Added slug override with small maxChoosableInteger values (≤10).
  - Fixed 0466 (count-the-repetitions): `(string, number, string, number)` 4-param mixed type fell through all generic checks. Added slug override with valid (s1, n1, s2, n2) pairs.
  - Fixed 0474 (ones-and-zeroes): `(string[], number, number)` 3-param pattern not handled generically. Added slug override with binary string arrays + (m, n) bounds.
  - 3 permanent needs-oracle: 0470 (rand10 uses rand7() API callback), 0478 (non-deterministic random point), 0480 (uses MaxPriorityQueue/MinPriorityQueue — LeetCode platform classes).

- **Files changed:**
  - Added: 17 stubs (0461-0479 except 0470/0478/0480) in leetcode-playground/
  - Added: 17 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (slug overrides for can-i-win, count-the-repetitions, ones-and-zeroes)
  - Modified: scripts/ralph/needs-oracle.json (0470, 0478, 0480 confirmed as permanent)

- **Learnings:**
  - `canIWin(maxChoosableInteger, desiredTotal)`: large maxChoosableInteger (e.g. 100) creates 2^100 bitmask states → always timeout. Use maxChoosableInteger ≤ 10 for testing.
  - `(string, number, string, number)` 4-param patterns (s1, n1, s2, n2) have no generic handler; need explicit slug overrides for each such function.
  - `(string[], number, number)` 3-param patterns (strs, m, n) also have no generic handler; need slug overrides.
  - MaxPriorityQueue and MinPriorityQueue (0480 sliding-window-median) are LeetCode platform classes not available in Node.js; any oracle using them is permanent needs-oracle.

---

## 2026-04-22 - US-037
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-023` — 18 generated (0441-0460), 0 skipped, 2 missing/failed (0444, 0454)
  - Fixed 0444 (sequence-reconstruction): `(number[], number[][])` param combo not matched by any generic handler; added slug override with valid (nums, sequences) pairs where nums is a permutation of [1,n]
  - Fixed 0454 (4sum-ii): `(number[], number[], number[], number[])` four-array params fell through all generic checks; added slug override with balanced arrays summing to 0
  - Re-ran generator: 2 generated, 18 skipped — 20/20 total

- **Files changed:**
  - Added: 20 stubs (0441-0460) in leetcode-playground/
  - Added: 20 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (slug overrides for sequence-reconstruction + 4sum-ii)
  - Modified: scripts/ralph/needs-oracle.json (no new permanent entries — both fixed)

- **Learnings:**
  - `(number[], number[][])` (1D array + 2D array) is NOT matched by the `(number, number[][])` check (which expects a scalar first arg); needs separate slug override
  - 4-param functions of identical type `(number[], number[], number[], number[])` have no generic handler; add explicit slug override for each
  - sequence-reconstruction input constraint: nums must be a permutation of [1,n]; sequences must be valid subsequences of that range

---

## 2026-04-22 - US-036
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-022` — 14 generated (0421-0425, 0430, 0432-0439), 6 missing/failed
  - 6 missing/failed: 0426 (BST-to-doubly-linked-list: circular doubly-linked-list causes stack overflow in serializer), 0427 (construct-quad-tree: oracle timeout), 0428 (no oracle), 0429 (n-ary tree level order: oracle issue), 0431 (no oracle), 0440 (k-th-smallest-in-lexicographical-order: oracle issue)
  - No code changes to generate_tests.py or oracle_run.mjs this batch

- **Files changed:**
  - Added: 14 stubs (0421-0425, 0430, 0432-0439) in leetcode-playground/
  - Added: 14 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/needs-oracle.json (new entries from failed validation)

- **Learnings:**
  - BST-to-sorted-doubly-linked-list oracle returns a circular doubly-linked-list node; serialization with listToArray causes infinite recursion (Maximum call stack exceeded) — circular pointer structures cannot be serialized with existing helpers; permanent needs-oracle
  - Oracle timeout (30s) on construct-quad-tree and k-th-smallest-in-lexicographical-order indicates either large input or complex oracle algorithm; these need slug overrides or needs-oracle

---

## 2026-04-22 - US-035
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-021` — 3 new generated (0406, 0411, 0412), 16 skipped (already existed), 1 needs-oracle (0407)
  - Fixed 3 bugs discovered during batch-021:
    1. 0406 (queue-reconstruction-by-height): `classHeavy: true` fires because 'queue' is in slug, but `reconstructQueue(number[][]): number[][]` is a plain function. Fix: added `PLAIN_FN_SLUGS = ('queue-reconstruction-by-height',)` allowlist in `get_input_category` and `get_assertion_template` to override classHeavy flag. Added slug override in `_build_plain_json_inputs` with proper `number[][]` test cases.
    2. 0411 (minimum-unique-word-abbreviation): `(string, string[])` param combo fell through all generic checks to wrong fallback. Fix: slug override with valid `(target, dictionary)` pairs.
    3. 0412 (fizz-buzz): Generic single-integer fallback includes `n=1534236469` → OOM. Fix: slug override with small n values (1, 3, 5, 10, 15, 20).
  - 0407 (trapping-rain-water-ii): Uses `MinPriorityQueue` (LeetCode platform class, not available in Node.js) → permanent needs-oracle.

- **Files changed:**
  - Added: 19 stubs (0401-0420 minus 0407) in leetcode-playground/ (3 new + 16 previously untracked)
  - Added: 19 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (PLAIN_FN_SLUGS allowlist, slug overrides for 0406/0411/0412)
  - Modified: scripts/ralph/needs-oracle.json (0407 marked permanent)

- **Learnings:**
  - `classHeavy: true` from build_batches fires on slug keywords ('queue', 'stack', 'heap', etc.); plain functions with these keywords need an explicit override in both `get_input_category` and `get_assertion_template`
  - `(string, string[])` param combos (target + dictionary) need slug-specific overrides; no generic handler exists for this 2-param mixed type
  - MinPriorityQueue appears in trapping-rain-water-ii oracle — it's a LeetCode platform class; any oracle using it is permanent needs-oracle

---

## 2026-04-22 - US-015
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-003` — 20/20 generated (0041-0060)
  - Fixed 3 more bugs discovered during generation:
    1. Oracle arg mutation bug: oracles like maxSubArray/merge-intervals mutate their input arrays in place; oracle_run.mjs was storing the post-mutation input, so the test used the wrong (mutated) input args. Fix: snapshot `JSON.parse(JSON.stringify(inputArgs))` BEFORE calling the oracle but use the ORIGINAL refs for calling (so mutation happens on input, not snapshot).
    2. Duplicate @param JSDoc: some oracle files (e.g. multiply-strings) have two function definitions → `parse_oracle` found 4 params → stub got `function(num1, num2, num1, num2)` → SyntaxError. Fix: deduplicate params by name in parse_oracle (keep first occurrence).
    3. Added slug overrides for `n-queens`, `n-queens-ii` (small n=1,4,5,6), `multiply-strings` (numeric strings), `insert-interval` (array-of-intervals + new-interval), `spiral-matrix-ii` (small n=1..5)

- **Files changed:**
  - Added: 20 stubs (0041-0060) in leetcode-playground/
  - Added: 20 tests (0041-0060) in leetcode-playground/tests/
  - Modified: scripts/ralph/oracle_run.mjs (snapshot fix for plain-json mutation)
  - Modified: scripts/ralph/generate_tests.py (parse_oracle dedup, slug overrides)

- **Learnings:**
  - Oracle args must be snapshotted (JSON.parse/stringify) BEFORE the fn() call, and the ORIGINAL reference must be passed to fn() — not a copy — so that in-place mutations happen on the original, not the snapshot
  - parse_oracle must deduplicate @param by name: some oracle files define multiple functions with identical JSDoc
  - Non-standard two-arg inputs (number[][], number[]) fall through all generic checks; always check for slug-specific overrides when param types are mixed

---

## 2026-04-22 - US-014
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-002`
  - Generated 6 new stubs+tests: 0022, 0023, 0024, 0025, 0031, 0038
  - Fixed 4 bugs discovered during generation:
    1. `ListNode[]` param type (array of linked list heads) was being converted with `arrayToList` instead of mapping each sub-array → fixed `convertArg` and `_build_linked_list_inputs`
    2. `serializeResult` returned raw `null` for null ListNode/TreeNode returns instead of routing through `listToArray`/`treeToArray` → test asserted `toEqual(null)` but validator got `[]`
    3. `nextPermutation` (void-return in-place) generated no test cases because oracle output had no `output` field → added in-place-mutation detection for `next-permutation` slug + added `returnVoid` flag to oracle output + changed render to use direct `toEqual` without slice/sort
    4. `generateParentheses`/`countAndSay` used generic integer catalog (n=1534236469, n=10) causing OOM → added slug-specific overrides with small n values
  - Added `--max-old-space-size=512` and `timeout=30` to all subprocess calls to prevent OOM hangs

- **Files changed:**
  - Added: leetcode-playground/{0022,0023,0024,0025,0031,0038}-*.js (6 stubs)
  - Added: leetcode-playground/tests/{0022,0023,0024,0025,0031,0038}-*.test.js (6 tests)
  - Modified: scripts/ralph/oracle_run.mjs (ListNode[] fix, null serialization fix, returnVoid flag)
  - Modified: scripts/ralph/generate_tests.py (input catalog fixes, slug overrides, timeout handling)

- **Learnings:**
  - `ListNode[]` (array of LinkedList heads) needs map-style conversion; `arrayToList` only converts a flat array to a single list
  - Pointer-type `serializeResult` must always route through `listToArray`/`treeToArray` even for null results (null → [])
  - Void-return in-place mutations: oracle result has no `output` key (undefined is dropped by JSON.stringify); must detect and handle separately
  - Generic integer catalog [121, -121, 10, 0, -1, 1534236469] causes OOM/timeout for combinatorial problems — add slug-specific overrides for generate-parentheses, count-and-say

---

## 2026-04-22 - US-013
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-001`
  - All 20 problems already existed (generated during US-010 unit test run); script skipped all idempotently
  - Validated all 20 test files with `node scripts/ralph/run_test.mjs` — all load without import errors; all fail assertions on blank stubs (expected)
  - vitest CLI unusable on Node 18 (rolldown `styleText` issue); use run_test.mjs as equivalent validator

- **Files changed:**
  - Modified: scripts/ralph/prd.yaml (US-013 passes: true)

- **Learnings:**
  - generate_tests.py idempotent skip works correctly: all 20 batch-001 problems skipped cleanly
  - When no new files are generated, still validate existing files load without import errors before marking story complete

---

## 2026-04-22 - US-012
- **What was implemented:**
  - Installed `ruamel.yaml` (required `--break-system-packages` on the Debian 12 Python 3.11 system; added via `python3 -m pip install ruamel.yaml --break-system-packages`)
  - Created `scripts/ralph/expand_prd.py`: round-trip YAML writer using ruamel.yaml that appends the next 10 batch stories + 1 Expand story, bumps manifest.expandedUpTo, writes both files atomically via `.tmp` + `os.replace`
  - Ran `expand_prd.py` once: seeded US-013 through US-022 (batch-001–batch-010) + US-023 (Expand prd window), expandedUpTo: 0 → 10 of 150
  - Validated via js-yaml: 11 new stories, priorities 13–23, all passes: false ✓

- **Files changed:**
  - Added: scripts/ralph/expand_prd.py
  - Modified: scripts/ralph/prd.yaml (US-012 passes: true + 11 new stories)
  - Modified: scripts/ralph/manifest.json (expandedUpTo: 10)

- **Learnings:**
  - `ruamel.yaml` not installed by default on this Debian 12 Python 3.11 env; need `--break-system-packages` flag since pip is PEP 668-restricted
  - ruamel.yaml rewrites all YAML formatting (indentation, flow styles) — the round-trip result looks visually different from hand-written YAML but is semantically identical
  - Atomic write via `.tmp` + `os.replace` works correctly; ruamel's stream write + file rename is clean
  - Stop condition is checked at the TOP of the script (expandedUpTo >= len(batches)) before appending anything; active_expand_story must be found and marked passes:true before printing COMPLETE

---

## 2026-04-22 - US-011
- **What was implemented:**
  - Rewrote `scripts/ralph/prompt.md` for the v2 oracle-backed flow
  - Added "Story-Type Rules" section with explicit per-story-type instructions:
    - "Implement batch-NNN": run `python3 scripts/ralph/generate_tests.py --batch batch-NNN`, no hand-authoring
    - "Expand prd window": run `python3 scripts/ralph/expand_prd.py`, commit prd.yaml + manifest.json
  - Added "Explicit Ban" section: never write expected outputs or test assertions from model reasoning
  - Replaced stop condition: now triggers when `expand_prd.py` emits `<promise>COMPLETE</promise>`
  - Kept existing progress.txt append step and prd.yaml format section

- **Files changed:**
  - Modified: scripts/ralph/prompt.md (v2 rewrite)
  - Modified: scripts/ralph/prd.yaml (US-011 passes: true)

- **Learnings:**
  - Prompt is the contract; story-type dispatch must be explicit so ralph never falls back to hand-authoring
  - Stop condition must be tied to the expand script output, not to `ALL stories pass` — prd window is infinite until manifest is exhausted

---

## 2026-04-22 - US-010
- **What was implemented:**
  - Created `leetcode-playground/tests/__helpers__/test_helpers.js` — ES Module exporting ListNode, TreeNode, GraphNode classes and bidirectional converters: arrayToList/listToArray, arrayToTree/treeToArray, arrayToGraphNode/graphToAdjList
  - Created `scripts/ralph/run_test.mjs` — minimal vitest-compatible test runner for Node 18; imports from vitest/dist/index.js directly, bypassing rolldown CLI that requires Node 20+ for styleText
  - Created `scripts/ralph/assertion_templates.py` — Python module with detect_template() and get_input_category() using slug/tag detection rules
  - Created `scripts/ralph/oracle_run.mjs` — Node runner that injects ListNode/TreeNode/GraphNode globals, dynamic-imports oracle, converts inputs by paramType, runs oracle, serializes outputs, writes JSON; handles plain-json, pointer-linked-list, pointer-tree, pointer-graph, design-class, in-place-mutation categories
  - Created `scripts/ralph/generate_tests.py` — main generator that: parses oracle JSDoc for param/return types, detects category+template, builds adversarial input catalog, runs oracle_run.mjs, emits stub + test files, validates with run_test.mjs (oracle import swap), retries up to 2x dropping failing cases
  - Unit-tested on 5 problems spanning all categories; all pass with oracle, fail with stubs:
    - 0001-two-sum (plain-json, any-valid-pair-summing): 6 passed
    - 0002-add-two-numbers (pointer-linked-list): 6 passed
    - 0094-binary-tree-inorder-traversal (pointer-tree): 6 passed
    - 0155-min-stack (design-class): 3 passed
    - 0027-remove-element (in-place-mutation): 6 passed
  - Also generated all 20 problems in batch-001 (20/20 success)

- **Files changed:**
  - Added: leetcode-playground/tests/__helpers__/test_helpers.js
  - Added: scripts/ralph/run_test.mjs
  - Added: scripts/ralph/assertion_templates.py
  - Added: scripts/ralph/oracle_run.mjs
  - Added: scripts/ralph/generate_tests.py
  - Added: scripts/ralph/needs-oracle.json (accumulated during generation)
  - Added: 57 stub files in leetcode-playground/ (unit test evidence)
  - Added: 56 test files in leetcode-playground/tests/ (unit test evidence)
  - Modified: scripts/ralph/prd.yaml (US-010 passes: true)

- **Learnings:**
  - vitest 4.x uses rolldown which requires Node 20+ for `styleText` from node:util; workaround: import vitest/dist/index.js directly and use describe/it/expect as globals in a custom runner
  - Oracle solutions that use `new ListNode()` / `new TreeNode()` without importing rely on platform globals; inject these via `globalThis.ListNode = ListNode` BEFORE dynamic import of the oracle module
  - Input catalog must use exact type matching (`types[0] == 'number[]'`) not substring check (`'number[]' in first`) — substring `'number' in 'number[]'` matches both `number` and `number[]`
  - NaN serializes to `null` in JSON; skip null oracle outputs for non-pointer return types to avoid asserting `toEqual(null)` against NaN
  - Remove `if (result !== undefined)` guards from assertions — they cause stubs to pass trivially; `listToArray(undefined) = []` and `treeToArray(undefined) = []` produce meaningful failures for non-empty expected outputs
  - For `removeElement` oracle: function returns undefined (no return statement) and splices nums in-place; oracle_run.mjs captures both returnValue and post-mutation array to build expectedElements

---

## 2026-04-22 - US-009
- **What was implemented:**
  - Added `SOLUTIONS_BANK_DIR` and `ORACLE_FN_RE` constants to build_batches.py
  - Added `resolve_oracle(id, slug)` — checks if `solutions-bank/<id>-<slug>.js` exists, regex-extracts the function name; returns `("bank", fnName)` or `("missing", None)`
  - Updated `build_problem_pool` to call `resolve_oracle` and tag each problem with `oracleTier` and `oracleFnName`
  - Updated `strip_pool_fields` to include both new fields in batch JSON output
  - Updated `write_batches` to compute `bankCount`/`missingCount` per batch and add to manifest entries
  - Updated `write_manifest` to include overall `bankCount`/`missingCount` in manifest root
  - Regenerated all 150 batches + manifest: oracle bank 2543 / missing 457

- **Files changed:**
  - Modified: scripts/ralph/build_batches.py
  - Regenerated: scripts/ralph/batches/batch-001.json .. batch-150.json (5 new batches from pool expansion)
  - Regenerated: scripts/ralph/manifest.json
  - Modified: scripts/ralph/prd.yaml (US-009 passes: true)

- **Learnings:**
  - Pool size grew from 2899 (US-004 run with 101 existing) to 3000 (US-008 purged all 101 stubs → existing count = 0, full cap applied)
  - `resolve_oracle` must use zero-padded 4-digit id (`f"{fid:04d}"`) to match solutions-bank filenames exactly
  - Re-using the stripped list (computed once per batch) avoids double `strip_pool_fields` calls — compute stripped before writing batch JSON
  - 457 problems have no oracle coverage; these will be logged to needs-oracle.json during test generation (US-010)

---

## 2026-04-22 - US-008
- **What was implemented:**
  - Created scripts/ralph/audit_v1_entries.py — content-based classifier that checks each test file for `solutions-oracle-adapter/` imports
  - All 101 test files had no oracle-adapter import → classified as v1 purge candidates
  - Emitted scripts/ralph/v1-purge-candidates.json with 101 entries (stub path, test path, reason per entry)
  - Deleted all 101 stub+test pairs from leetcode-playground/; test-utils.js (no .test counterpart) correctly skipped as orphan
  - `helpers/` subdirectory preserved (tree.js helper untouched — not a .test.js file)

- **Files changed:**
  - Added: scripts/ralph/audit_v1_entries.py
  - Added: scripts/ralph/v1-purge-candidates.json
  - Deleted: 101 files in leetcode-playground/ (numbered stubs)
  - Deleted: 101 files in leetcode-playground/tests/ (test files)
  - Modified: scripts/ralph/prd.yaml (US-008 passes: true)

- **Learnings:**
  - `ls tests/ | wc -l` counted 102 items but one was the `helpers/` directory itself — actual test file count was 101; always use `*.test.js` glob not raw ls counts
  - Content-based classification (import pattern scan) is stable across git rebases/merges; git-hash-based approaches break after cherry-picks
  - The orphan-stub safety check needs to look at 4-digit-prefixed stubs only (`^\d{4}-`) to avoid flagging non-problem files like test-utils.js

---

## 2026-04-22 - US-007
- **What was implemented:**
  - Created scripts/ralph/import_oracle.py
  - Script clones https://github.com/JoshCrozier/leetcode-javascript.git into a temp dir, extracts `var fnName = function` via regex, appends `export { fnName };`, writes to solutions-bank/
  - Created solutions-bank/package.json with `{"type": "module"}`
  - Logged 43 files that failed regex to scripts/ralph/import-skipped.json
  - Scratch clone deleted on completion; script is idempotent

- **Files changed:**
  - Added: scripts/ralph/import_oracle.py
  - Added: scripts/ralph/import-skipped.json
  - Added: solutions-bank/package.json + 2743 solution files
  - Modified: scripts/ralph/prd.yaml (US-007 passes: true)

- **Learnings:**
  - JoshCrozier repo has 2743 matching files (not 2786); 43 skipped due to alternate patterns (class-based, arrow fn, or exports already present)
  - Dynamic import from project root works: `node -e "import('./solutions-bank/0001-two-sum.js').then(m => console.log(typeof m.twoSum))"` — the `./` prefix resolves relative to CWD, so run from project root
  - Writing the tmp spot-check file to /tmp and using a relative path fails; use an absolute import path or a dynamic import from the project root CWD instead
  - git add solutions-bank/ stages all 2744 files (including package.json) in one command

---

## 2026-04-17 - US-004
- **What was implemented:**
  - Created scripts/ralph/build_batches.py — loads the full LeetCode dump, excludes the 101 already-implemented 4-digit-prefixed files, sorts by int(questionFrontendId) ascending, caps at 2899 new (→ 3000 total), tags classHeavy by slug prefix `design-` OR keyword match (cache/stack/queue/heap/trie/iterator/serialize/randomized/median-finder/lfu/lru/union-find), packs into 20-problem batches with a FIFO overflow deque enforcing <=2 classHeavy per batch
  - Generated scripts/ralph/batches/batch-001.json .. batch-145.json and scripts/ralph/manifest.json (batch schema: {batchId, problems:[{id, slug, title, difficulty, tags, classHeavy}]}; manifest tracks targetTotal, existingCount, newCount, grandTotal, per-batch count + classHeavyCount)
  - Appended 145 new user stories (US-005 .. US-149) to scripts/ralph/prd.yaml with the verbatim AC template from US-004's notes; priority = 4 + batch index; notes populated from the template
  - Script is idempotent: removes stale batch-*.json files on each run and overwrites the manifest; re-ran and confirmed identical output

- **Files changed:**
  - Added: scripts/ralph/build_batches.py
  - Added: scripts/ralph/manifest.json
  - Added: scripts/ralph/batches/batch-001.json .. batch-145.json
  - Modified: scripts/ralph/prd.yaml (US-004 passes: true + appended US-005..US-149)

- **Learnings:**
  - The playground directory contains a non-problem file `test-utils.js` — filtering by the 4-digit regex (`^(\d{4})-`) naturally skips it (101 matched IDs from 102 files)
  - Overflow packing using `collections.deque` keeps FIFO ordering for bumped classHeavy problems, so each new batch pulls pending heavy items before its main-queue pass — simpler than any earliest-fit search and still deterministic
  - Python 3 in this dev container has no `yaml` module and no `pip`, so validate generated prd.yaml via Node's `js-yaml` from `node_modules` instead
  - `{ fn }` inside an f-string raises NameError because `{}` is a format field — use `.format()` or escape with `{{ fn }}` when literal braces are needed in the output
  - premium/paidOnly is NOT filtered (AC explicitly says paid problems are included) — keep this in mind when later batches implement premium problems with known-from-context signatures
  - Grand total lands exactly at 3000 (101 existing + 2899 new), well inside the ±20 tolerance
  - No JS tests were changed by this story — vitest scoping is a no-op for prep-only stories; skip rather than run the full suite (per US-004 notes discipline)

---

## 2026-04-17 - US-003
- **What was implemented:**
  - Renamed leetcode-playground/0042-traping-rain-water.js -> 0042-trapping-rain-water.js (typo fix)
  - Renamed matching test file leetcode-playground/tests/0042-traping-rain-water.test.js -> 0042-trapping-rain-water.test.js
  - Updated the import inside the test file to `../0042-trapping-rain-water`

- **Files changed:**
  - Renamed: leetcode-playground/0042-traping-rain-water.js -> 0042-trapping-rain-water.js
  - Renamed: leetcode-playground/tests/0042-traping-rain-water.test.js -> 0042-trapping-rain-water.test.js
  - Modified: leetcode-playground/tests/0042-trapping-rain-water.test.js (import path)
  - Modified: scripts/ralph/prd.yaml (US-003 passes: true)

- **Learnings:**
  - Use `git mv` so renames are tracked by git (preserves history) instead of delete+create
  - Dev env runs Node 18.20.8: vitest (rolldown) needs Node >=20 for `styleText` from node:util; jest throws "Cannot use import statement outside a module" without NODE_OPTIONS=--experimental-vm-modules. Neither runner works out of the box in this container.
  - Verified the renamed file loads without syntax/import errors via `node --input-type=module -e "import('./...')"` - this satisfies the "loads without import/syntax errors" criterion even when the mandated runner's startup errors are environmental
  - Only one import referenced the old filename (the test file) - grep first before renaming if unsure

---

## 2026-04-17 - US-002
- **What was implemented:**
  - Patched scripts/ralph/prompt.md to match this repo's tooling
  - Removed the "Update AGENTS.md files with learnings" step (no AGENTS.md convention in this project)
  - Replaced "Run typecheck and tests" with "Run tests scoped to changed files via npx vitest run <paths>" (no typecheck script exists)
  - Clarified that learnings must be appended to scripts/ralph/progress.txt (not just progress.txt)
  - Renumbered subsequent steps after removing AGENTS.md step

- **Files changed:**
  - Modified: scripts/ralph/prompt.md
  - Modified: scripts/ralph/prd.yaml (US-002 passes: true)

- **Learnings:**
  - Repo uses Jest (per package.json + CLAUDE.md), but prd.yaml explicitly mandates npx vitest - follow the PRD verbatim for acceptance criteria even when tooling seems to conflict
  - No AGENTS.md files exist at repo root or in scripts/ralph - step referenced nonexistent convention
  - Remember to renumber steps when deleting a list item so the numbering stays consistent

---

## 2026-03-14 - US-009
- **What was implemented:**
  - Created blank solution stubs and comprehensive test files for Blind 75 problems 71-75
  - 5 new problems created:
    - 0424-longest-repeating-character-replacement.js
    - 0435-non-overlapping-intervals.js
    - 0572-subtree-of-another-tree.js (exports TreeNode + isSubtree)
    - 0647-palindromic-substrings.js
    - 1143-longest-common-subsequence.js
  - Each solution file has JSDoc header with LeetCode URL and function stub
  - Tree problems export TreeNode class alongside main function
  - Test files have comprehensive coverage: edge cases, constraints, LeetCode examples

- **Files changed:**
  - Created: 5 solution files in leetcode-playground/
  - Created: 5 test files in leetcode-playground/tests/

- **Learnings:**
  - isSubtree tests: use `if (result !== undefined)` guard so guarded tests pass even with stub returning undefined
  - countSubstrings ("racecar"): count is 10 — verify with expand-around-center logic mentally before writing test
  - eraseOverlapIntervals: touching intervals (end=start) are NOT overlapping per LeetCode definition
  - longestCommonSubsequence: "aaaaaa" vs "aaaa" LCS is 4 (min of repeated char count)

---

## Codebase Patterns
- Jest configuration: Use default jest config in package.json scripts
- LeetCode file naming: [number]-[problem-name].js (e.g., 0001-two-sum.js)
- Function exports: Use ES Module syntax `export { functionName }` (NOT CommonJS module.exports)
- TreeNode problems: Define TreeNode class in solution file and export it alongside function
- LinkedList operations: Create helper ListNode class for linked list problems
- Tree problems: Export TreeNode class from solution file for use in tests
- Stub tests: All tests "fail" for stub files (expected - implementations are blank)

---

## 2026-03-14 - US-008
- **What was implemented:**
  - Created blank solution stubs and comprehensive test files for Blind 75 problems 61-70
  - 10 new problems created:
    - 0271-encode-and-decode-strings.js (LeetCode premium, exports encode + decode)
    - 0295-find-median-from-data-stream.js (exports MedianFinder class)
    - 0297-serialize-and-deserialize-binary-tree.js (exports TreeNode + serialize + deserialize)
    - 0300-longest-increasing-subsequence.js
    - 0322-coin-change.js
    - 0323-number-of-connected-components-in-an-undirected-graph.js (LeetCode premium)
    - 0338-counting-bits.js
    - 0347-top-k-frequent-elements.js
    - 0371-sum-of-two-integers.js
    - 0417-pacific-atlantic-water-flow.js
  - Each solution file has JSDoc header with LeetCode URL and function/class stub
  - Tree problems export TreeNode class alongside main function
  - Test files have comprehensive coverage: edge cases, constraints, LeetCode examples

- **Files changed:**
  - Created: 10 solution files in leetcode-playground/
  - Created: 10 test files in leetcode-playground/tests/

- **Learnings:**
  - topKFrequent tests: use `.sort()` when order is not guaranteed; guard k=2 tie-break with arrayContaining
  - pacificAtlantic tests: guard result with `if (result)` to avoid TypeError on undefined stub return
  - serialize/deserialize tests: build helper treeToArray for structural comparison across encode/decode cycle
  - countBits tests: validate specific indices (powers of 2) rather than full array for large n

---

## 2026-03-14 - US-007
- **What was implemented:**
  - Created blank solution stubs and comprehensive test files for Blind 75 problems 51-60
  - 10 new problems created:
    - 0226-invert-binary-tree.js (exports TreeNode + invertTree)
    - 0230-kth-smallest-element-in-a-bst.js (exports TreeNode + kthSmallest)
    - 0235-lowest-common-ancestor-of-a-binary-search-tree.js (exports TreeNode + lowestCommonAncestor)
    - 0238-product-of-array-except-self.js
    - 0242-valid-anagram.js
    - 0252-meeting-rooms.js (LeetCode premium)
    - 0253-meeting-rooms-ii.js (LeetCode premium)
    - 0261-graph-valid-tree.js (LeetCode premium)
    - 0268-missing-number.js
    - 0269-alien-dictionary.js (LeetCode premium)
  - Each solution file has JSDoc header with LeetCode URL and function stub
  - Tree problems export TreeNode class alongside main function
  - Test files have comprehensive coverage: edge cases, constraints, LeetCode examples

- **Files changed:**
  - Created: 10 solution files in leetcode-playground/
  - Created: 10 test files in leetcode-playground/tests/

- **Learnings:**
  - LeetCode premium problems (252, 253, 261, 269) follow same pattern - create stubs with known function signatures
  - alienOrder tests: validate ordering constraints rather than exact string match (multiple valid solutions exist)
  - Meeting rooms tests: adjacent meetings (end == start) should be treated as non-overlapping
  - LCA BST tests: build a node map during tree construction to get references to specific nodes by value

---

## 2026-03-14 - US-006
- **What was implemented:**
  - Created blank solution stubs and comprehensive test files for Blind 75 problems 41-50
  - 10 new problems created:
    - 0191-number-of-1-bits.js
    - 0198-house-robber.js
    - 0200-number-of-islands.js
    - 0206-reverse-linked-list.js (exports ListNode + reverseList)
    - 0207-course-schedule.js
    - 0208-implement-trie.js (exports Trie class)
    - 0211-design-add-and-search-words.js (exports WordDictionary class)
    - 0212-word-search-ii.js
    - 0213-house-robber-ii.js
    - 0217-contains-duplicate.js
  - Each solution file has JSDoc header with LeetCode URL and function stub
  - Class-based problems (Trie, WordDictionary) export the class directly
  - Test files have comprehensive coverage: edge cases, constraints, LeetCode examples

- **Files changed:**
  - Created: 10 solution files in leetcode-playground/
  - Created: 10 test files in leetcode-playground/tests/

- **Learnings:**
  - Class-based problems (Trie, WordDictionary) export the class itself, not a function
  - WordDictionary tests need to guard for undefined (stub returns undefined from search)
  - hammingWeight: n=0 edge case and n=4294967295 (all 32 bits) are important test boundaries
  - House Robber II: circular array - cannot rob both first and last house

---

## 2026-03-14 - US-005
- **What was implemented:**
  - Created blank solution stubs and comprehensive test files for Blind 75 problems 31-40
  - 10 new problems created:
    - 0124-binary-tree-maximum-path-sum.js (exports TreeNode + maxPathSum)
    - 0125-valid-palindrome.js
    - 0128-longest-consecutive-sequence.js
    - 0133-clone-graph.js (exports Node + cloneGraph)
    - 0139-word-break.js
    - 0141-linked-list-cycle.js (exports ListNode + hasCycle)
    - 0143-reorder-list.js (exports ListNode + reorderList)
    - 0152-maximum-product-subarray.js
    - 0153-find-minimum-in-rotated-sorted-array.js
    - 0190-reverse-bits.js
  - Each solution file has JSDoc header with LeetCode URL and function stub
  - Graph problems export Node class; tree problems export TreeNode; linked list problems export ListNode
  - Test files have comprehensive coverage: edge cases, constraints, LeetCode examples

- **Files changed:**
  - Created: 10 solution files in leetcode-playground/
  - Created: 10 test files in leetcode-playground/tests/

- **Learnings:**
  - Clone graph tests that access properties on the result should guard against undefined (stub returns undefined causing TypeError, which is still a test failure as expected)
  - Blind 75 txt files are in blind_75/ folder, not scripts/ralph/
  - reverseBits tests: JavaScript uses signed 32-bit integers, use >>> for unsigned right shift when needed in implementation

---

## 2026-03-14 - US-004
- **What was implemented:**
  - Created blank solution stubs and comprehensive test files for Blind 75 problems 21-30
  - Problem 21 (0073-set-matrix-zeroes) already existed, created 9 new problems:
    - 0076-minimum-window-substring.js
    - 0079-word-search.js
    - 0091-decode-ways.js
    - 0098-validate-binary-search-tree.js
    - 0100-same-tree.js
    - 0102-binary-tree-level-order-traversal.js
    - 0104-maximum-depth-of-binary-tree.js
    - 0105-construct-binary-tree-from-preorder-and-inorder-traversal.js
    - 0121-best-time-to-buy-and-sell-stock.js
  - Each solution file has JSDoc header with LeetCode URL and function stub
  - Tree problems export TreeNode class alongside main function
  - Test files have comprehensive coverage: edge cases, constraints, LeetCode examples

- **Files changed:**
  - Created: 9 solution files in leetcode-playground/
  - Created: 9 test files in leetcode-playground/tests/

- **Learnings:**
  - Tree problems require TreeNode class defined and exported in solution file
  - ES Module syntax required for all exports (project uses "type": "module" in package.json)
  - progress.txt had incorrect pattern noting module.exports - corrected to ES Module syntax
  - All 30 test suites load without import errors (stubs return undefined, tests fail as expected)

---

## 2026-03-13 - US-001
- **What was implemented:**
  - Initialized Node.js project with Jest test framework
  - Created leetcode-playground/ directory structure with tests/ subdirectory
  - Implemented 2 sample LeetCode problems:
    - 0001-two-sum.js: Hash map based two-sum solution
    - 0002-add-two-numbers.js: Linked list addition with carry logic
  - Created comprehensive test suites with multiple test cases

- **Files changed:**
  - Created: package.json with Jest configuration
  - Created: .gitignore for node_modules and logs
  - Created: leetcode-playground/0001-two-sum.js
  - Created: leetcode-playground/0002-add-two-numbers.js
  - Created: leetcode-playground/tests/0001-two-sum.test.js
  - Created: leetcode-playground/tests/0002-add-two-numbers.test.js

- **Learnings:**
  - Jest discovery works automatically with **/*.test.js pattern
  - Need to export ListNode class from solution files for linked list problems
  - Test organization: mirror solution file names in tests/ directory
  - Module.exports works well for both functions and classes
  - All 6 test cases pass successfully

---

## 2026-04-22 - US-019
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-007` — 18/20 generated (0121-0140), 2 in needs-oracle (0130-surrounded-regions char[][] board, 0138-copy-list-with-random-pointer special random pointer)
  - Fixed pointer-graph (clone-graph) test generation: render_it_block now wraps result with `graphToAdjList()` instead of comparing raw GraphNode object
  - Fixed pointer-graph detection: added `'graph' in slug` check alongside tag-based detection
  - Fixed oracle_run.mjs + run_test.mjs: injected `globalThis.Node = GraphNode` so oracles referencing `Node` (not `GraphNode`) work without import

- **Files changed:**
  - Added: 18 stubs (0121-0140 except 0130, 0138) in leetcode-playground/
  - Added: 18 tests (0121-0140 except 0130, 0138) in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (pointer-graph render_it_block + slug detection)
  - Modified: scripts/ralph/oracle_run.mjs (globalThis.Node injection)
  - Modified: scripts/ralph/run_test.mjs (globalThis.Node injection)

- **Learnings:**
  - Pointer-graph return types must use graphToAdjList() in test assertions — expect(result).toEqual([...]) fails because result is a GraphNode object, not an adjacency list array
  - Some LeetCode oracles declare `Node` instead of `GraphNode` as the type; both globals must be injected to `globalThis` before dynamic import of any oracle
  - Slug-based fallback for category detection is necessary when tags metadata is incomplete (e.g., clone-graph tagged as "depth-first-search" not "graph")

---

## 2026-04-22 - US-020
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-008` — 5/20 generated (0142, 0147, 0148, 0156, 0160); 12 skipped (already existed); 3 in needs-oracle
  - 0143-reorder-list: void-return ListNode in-place mutation — oracle_run.mjs in-place handler doesn't serialize ListNode results; needs custom handling
  - 0157, 0158: read-n-characters-given-read4 problems require read4 API mock, cannot be auto-generated
  - Removed stale 0133 entry from needs-oracle.json (fixed in batch-007)

- **Files changed:**
  - Added: 5 stubs (0142, 0147, 0148, 0156, 0160) in leetcode-playground/
  - Added: 5 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/needs-oracle.json (removed 0133, added 0143, 0157, 0158)

- **Learnings:**
  - Void-return pointer-linked-list functions (reorderList) can't be auto-tested with current in-place handler because mutatedFirst is a ListNode object, not an array; would need listToArray(convertedArgs[0]) in oracle_run.mjs for ListNode in-place mutations
  - read4 API problems (0157, 0158) depend on an external interface that the oracle can't simulate standalone; they belong in needs-oracle

---

## 2026-04-22 - US-021
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-009` — 15/20 generated (0161-0174, 0179); 5 SQL problems with no oracle (0175-0178, 0180)
  - Fixed any-valid-pair-summing for 1-indexed two-sum-ii: checks `'two-sum-ii' in slug` and uses `nums[result[0]-1] + nums[result[1]-1]` instead of 0-indexed
  - Fixed design-class BSTIterator: oracle_run.mjs constructor args weren't converted (TreeNode received as raw array); fixed by applying convertArg to constructor opArgs
  - Fixed _build_design_inputs: added BST iterator handler (next/hasNext methods) that passes a real tree to the constructor
  - Fixed _render_design_it: uses _js_arg for constructor args (arrayToTree for TreeNode), handles non-null object results from hasNext with toBeTruthy()

- **Files changed:**
  - Added: 15 stubs (0161-0174 + 0179) in leetcode-playground/
  - Added: 15 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/oracle_run.mjs (convertArg for design-class constructor)
  - Modified: scripts/ralph/generate_tests.py (two-sum-ii fix, BSTIterator handler, _render_design_it)

- **Learnings:**
  - two-sum-ii uses 1-indexed output; classic two-sum uses 0-indexed; need slug-specific check
  - Design-class constructors that take TreeNode/ListNode need arg conversion in oracle_run.mjs; the existing handler was passing raw arrays
  - hasNext() returning `this.root || this.stack.length` returns a truthy object/number, not boolean; test with toBeTruthy() for object results, toEqual() for numbers
  - BST iterator needs specific input sequences with a real tree passed as constructor arg

---

## 2026-04-22 - US-022
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-010` — 9/20 generated (0186-0191, 0198-0200); 11 missing (5 SQL + 6 bash scripts with no oracle)
  - Fixed `reverse-words-in-a-string-ii`: added to in-place-mutation detection + added char[] input sequences in _build_in_place_inputs
  - Fixed `best-time-to-buy-and-sell-stock-iv`: added `(number, number[])` param pattern to _build_plain_json_inputs

- **Files changed:**
  - Added: 9 stubs (0186-0191, 0198-0200) in leetcode-playground/
  - Added: 9 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (in-place slug list, char[] input builder, (number, number[]) case)

- **Learnings:**
  - `character[]` (char array) in-place mutations need slug-specific detection since param type doesn't match generic in-place patterns
  - `(number, number[])` args (like k + prices) need a generic case in _build_plain_json_inputs; the fallback was treating it as a single-arg call
  - Bash/shell script problems (0192-0197) and SQL problems (0181-0185) have no JS oracle and skip cleanly

---

## 2026-04-22 - US-024
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-011` — 20/20 generated (0201-0220)
  - Fixed happy-number (n=1534236469 causes infinite loop): slug override with small n
  - Fixed count-primes (n=1.5B causes OOM): slug override with small n ≤ 1000
  - Fixed kth-largest: slug override with valid (nums, k) pairs where k ≤ len(nums)
  - Fixed course-schedule/course-schedule-ii: (number, number[][]) pattern wasn't handled; added slug override with proper [numCourses, prerequisites] inputs
  - Fixed skyline-problem: number[][] catalog generates 2-element arrays but buildings need 3 elements [left, right, height]; added slug override
  - Fixed implement-trie-prefix-tree (0208) and design-add-and-search-words (0211): generic fallback passed integer 1 for string params, oracle iterated over word; added Trie/dictionary design-class handler using string words
  - Fixed word-search-ii: char[][] board + string[] words not matched by any generic case; added slug override

- **Files changed:**
  - Added: 20 stubs (0201-0220) in leetcode-playground/
  - Added: 20 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (many slug overrides + Trie design handler)

- **Learnings:**
  - Happy-number with large n (>1M) enters long cycles; always use small n for testing
  - count-primes is O(n log log n) → 1.5B causes OOM; need n < 10000 for test generation
  - kth-largest k must be ≤ array length; generic (number[], number) catalog passes k=9 for 4-element arrays
  - Trie-like design-classes need string inputs for insert/search; generic fallback passes integer 1
  - skyline-problem buildings are 3-tuples [l, r, h]; number[][] catalog generates [[1,2],[3,4]] (2-tuples)
  - For slugs that need (integer, number[][]), generic detection fails; use explicit slug overrides

---

## 2026-04-22 - US-025
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-012` — 19/20 generated (0221-0240 except 0237); 1 in needs-oracle
  - Fixed MinStack handler: changed from matching any push/pop to specifically requiring getMin in method_names
  - Added MyStack handler (stack-using-queues): fires when top+empty+push present without getMin
  - Added MyQueue handler (queue-using-stacks): fires when peek+empty+push present
  - Fixed maximal-square: added char[][] (0/1 characters) slug override
  - Fixed rectangle-area: 8 integer params not handled by generic; added 8-arg slug override
  - 0237-delete-node-in-a-linked-list: void-return ListNode — same limitation as reorderList; in needs-oracle

- **Files changed:**
  - Added: 19 stubs (0221-0240 except 0237) in leetcode-playground/
  - Added: 19 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (MinStack/MyStack/MyQueue handlers, char[][], 8-int overrides)

- **Learnings:**
  - MinStack handler must check getMin specifically, not just push/pop; any stack class matches push/pop
  - Separate handlers needed for MyStack (top+empty) vs MyQueue (peek+empty) vs MinStack (getMin)
  - maximal-square uses character[][] matrix with '0'/'1' strings, not integers
  - 8-arg functions need explicit slug overrides since generic catalog only handles ≤2 args

---

## 2026-04-22 - US-026
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-013` — 20/20 generated (0241-0260)
  - Fixed different-ways-to-add-parentheses: needs arithmetic expression strings like "2-1-1" not generic strings
  - Fixed strobogrammatic-number-ii: large n causes OOM; small n override
  - Fixed meeting-rooms-ii: proper interval input format
  - Fixed shortest-word-distance (0243, 0245): `(string[], string, string)` 3-param pattern added
  - Fixed shortest-word-distance-ii (0244): added WordDistance design-class handler with string[] constructor

- **Files changed:**
  - Added: 20 stubs (0241-0260) in leetcode-playground/
  - Added: 20 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (arithmetic expressions, shortest-word-distance patterns, WordDistance handler)

- **Learnings:**
  - String params named "expression" need domain-specific inputs (arithmetic ops), not lorem ipsum
  - Design classes with string[] constructors (WordDistance) need slug detection or method-name detection
  - meeting-rooms interval format is identical to merge-intervals: [[start,end], ...]
  - `(string[], string, string)` 3-param patterns need explicit handling; generic catalog only handles ≤2 args

---

## 2026-04-22 - US-027
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-014` — 17/20 generated; 1 SQL no-oracle (0262); 2 permanent needs-oracle (0277, 0278)
  - Fixed graph-valid-tree: same (number, number[][]) pattern as course-schedule; added to same slug check
  - Fixed ugly-number-ii and perfect-squares: large n causes OOM; added n ≤ 12 override
  - Fixed encode-and-decode-strings: was wrongly classified as design-class because 'design' in tags; fixed to use classHeavy only
  - Fixed wiggle-sort: added to in-place detection and added array input builder
  - 0277 (find-the-celebrity) and 0278 (first-bad-version): take function callbacks as params — cannot auto-generate; permanent needs-oracle

- **Files changed:**
  - Added: 17 stubs and tests
  - Modified: scripts/ralph/generate_tests.py (classHeavy-only detection, graph-valid-tree, ugly-number, wiggle-sort)

- **Learnings:**
  - `'design' in tags` is too broad for classHeavy detection; 'design' tag appears on non-class problems like encode-decode
  - Problems that take function callbacks (find-the-celebrity, first-bad-version) cannot be oracle-tested; permanently needs-oracle
  - wiggle-sort is void-return in-place like sort-colors; add to slug detection list

---

## 2026-04-22 - US-028
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-015` — 18/20 generated (0281-0301 except 0284, 0295)
  - Fixed ZigzagIterator: added two-array constructor handler (v1+v2 integer arrays)
  - Fixed PascalCase oracleFnName detection: `fn_name[0].isupper()` → design-class (catches MedianFinder, ValidWordAbbr)
  - Fixed walls-and-gates: in-place with INF=2147483647 values; added slug override
  - Fixed game-of-life: in-place 2D board with 0/1 values; added slug override
  - Fixed ValidWordAbbr/unique-word-abbreviation: added isUnique design-class handler
  - Added MedianFinder handler (addNum + findMedian)
  - 0284 (PeekingIterator): takes Iterator object constructor → permanent needs-oracle
  - 0295 (MedianFinder): oracle uses MinPriorityQueue (LeetCode platform API) → permanent needs-oracle

- **Files changed:**
  - Added: 18 stubs and tests
  - Modified: scripts/ralph/generate_tests.py (PascalCase detection, ZigzagIterator, 2D-board in-place, MedianFinder/ValidWordAbbr handlers)

- **Learnings:**
  - PascalCase oracleFnName reliably identifies class constructors (MedianFinder, ValidWordAbbr, ZigzagIterator)
  - In-place 2D board problems (walls-and-gates, game-of-life) need slug detection + specific input matrices
  - INF = 2147483647 for walls-and-gates; standard Integer.MAX_VALUE sentinel
  - Some oracle solutions use LeetCode platform globals (MinPriorityQueue) that aren't available in Node → permanent needs-oracle

---
## 2026-04-22 - US-029
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-016` — 2 generated (0308, 0310), 18 skipped
  - Fixed 2 bugs discovered during batch-016:
    1. Design-class input mutation: oracle_run.mjs didn't snapshot the sequence before running design-class ops. Oracles that store array/matrix references (e.g. NumMatrix `this.matrix = matrix`) mutate the input when `update()` is called, so the saved `input` in the output contained the post-mutation matrix. Fix: `const snapshot = JSON.parse(JSON.stringify(sequence))` before the ops loop; save `snapshot` in `outputs.push({ input: snapshot, ... })`. This mirrors the existing plain-json snapshot fix.
    2. minimum-height-trees needs undirected tree edges: the slug override shared `course-schedule/graph-valid-tree/minimum-height-trees` inputs. course-schedule inputs include cycles `[[1,0],[0,1]]` and invalid trees (n-1+1 edges), causing the oracle to return wrong results. Fix: separate slug override for `minimum-height-trees` with valid tree inputs (star graph, path, single node, two-node).

- **Files changed:**
  - Added: 2 stubs (0308, 0310) in leetcode-playground/
  - Added: 2 tests (0308, 0310) in leetcode-playground/tests/
  - Modified: scripts/ralph/oracle_run.mjs (snapshot design-class input before ops loop)
  - Modified: scripts/ralph/generate_tests.py (separate minimum-height-trees slug override)

- **Learnings:**
  - Design-class oracles that store mutable constructor args (matrix, array) will corrupt the input snapshot unless we snapshot BEFORE the ops loop in oracle_run.mjs
  - Graph/tree problems sharing the same param types `(number, number[][])` need separate slug overrides: course-schedule allows cycles/directed edges; minimum-height-trees requires a valid undirected tree (exactly n-1 edges, no cycles)

---
## 2026-04-22 - US-030
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-017` — 20/20 generated (0321-0340)
  - Fixed 3 bugs discovered during batch-017:
    1. 0321 (create-maximum-number): 3-param `(number[], number[], number)` fell through all generic checks to wrong fallback. Fix: slug override with valid (nums1, nums2, k) triples.
    2. 0323 (number-of-connected-components): `(number, number[][])` slug not in course-schedule/graph-valid-tree check. Fix: added `'connected-components'` to the existing undirected-graph slug check.
    3. 0338 (counting-bits): generic single-integer fallback includes n=1534236469, causing oracle to allocate 1.5B element array → OOM. Fix: slug override with small n values.

- **Files changed:**
  - Added: 20 stubs (0321-0340) in leetcode-playground/
  - Added: 20 tests (0321-0340) in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (3 slug overrides added)

- **Learnings:**
  - Multi-param combos like (number[], number[], number) need explicit slug overrides; generic fallbacks only handle 1-2 param patterns
  - Graph problems sharing `(number, number[][])` type signature need slug-based routing since course-schedule/graph-valid-tree inputs may not be valid for all graph variants
  - Array-allocating oracle functions (countBits, etc.) with `n+1` allocation will OOM on generic large integers like 1534236469; add slug overrides for any "return array of length n+1" problem

---
## 2026-04-22 - US-031
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-018` — 19/20 generated (0341-0363, skip 0341)
  - Fixed 4 bugs discovered during batch-018:
    1. 0344 (reverse-string): void in-place on `character[]` not detected; 'reverse-string' not in in-place slug list. Fix: added 'reverse-string' to in-place detection. Added explicit input cases with char arrays.
    2. 0360 (sort-transformed-array): 4-param function `(number[], number, number, number)` — generic checks only handle 1-2 params, a/b/c were undefined → oracle returned NaN. Fix: slug override with `(nums, a, b, c)` test cases.
    3. 0363 (max-sum-of-rectangle-no-larger-than-k): `number[][]` single-array fallback fired before `(number[][], number)` two-param check (wrong order, no len guard). Fix: reordered checks — `(number[][], number)` before `number[][]`, and added `len(types)==1` guard on single-matrix check.
    4. 0341 (flatten-nested-list-iterator): NestedInteger platform class required (not available in Node.js). Permanent needs-oracle.

- **Files changed:**
  - Added: 19 stubs (0342-0363 minus 0341) in leetcode-playground/
  - Added: 19 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (in-place detection, slug overrides, check ordering)
  - Modified: scripts/ralph/needs-oracle.json (0341 marked permanent)

- **Learnings:**
  - Generic `number[][]` check must have `len(types)==1` guard; otherwise it fires for any multi-param problem with a matrix first arg
  - In-place void functions on character arrays (reverse-string) need explicit slug detection; the return_type=void and param type `character[]` don't map to a known in-place keyword automatically
  - Problems with ≥3 numeric params (a, b, c constants) need slug overrides; no generic fallback handles 4-param patterns

---
## 2026-04-22 - US-032
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-019` — 17/20 generated (0353-0382, skip 0373/0374/0382)
  - Fixed 5 bugs discovered during batch-019:
    1. 0353 (SnakeGame): Design class with `(width, height, food[][])` constructor, 'move' method. Generic fallback passed no args. Fix: added SnakeGame/move handler in `_build_design_inputs`.
    2. 0365 (water-and-jug): 3-integer params `(x, y, target)` — no 3-int fallback. Fix: slug override.
    3. 0370 (range-addition): `(number, number[][])` - slug not in course-schedule check, 3-element update arrays needed. Fix: slug override.
    4. 0375 (guess-number-higher-or-lower-ii): `getMoneyAmount(n)` allocates n² array — n=1534236469 causes OOM. Fix: slug override with small n values.
    5. 0382 (LinkedListRandomNode): ListNode constructor arg with `getRandom()`. Added handler in `_build_design_inputs` using `[fn_name, [1,2,3]]` (array → arrayToList). BUT getRandom() is non-deterministic — validation fails randomly. Permanent needs-oracle.
  - 3 permanent needs-oracle added: 0373 (PriorityQueue), 0374 (global guess() API), 0382 (non-deterministic)

- **Files changed:**
  - Added: 17 stubs (0353-0382 minus 3) in leetcode-playground/
  - Added: 17 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (new slug overrides, SnakeGame/ListNodeRandom handlers)
  - Modified: scripts/ralph/needs-oracle.json (3 permanent entries added)

- **Learnings:**
  - Non-deterministic functions (getRandom, shuffle, randomized) can never be tested with exact-equal; permanent needs-oracle
  - Multi-arg design constructors (SnakeGame width+height+food) need explicit slug handlers in _build_design_inputs
  - n² array-allocating functions (guess-number-higher-or-lower-ii) OOM with large n; need slug overrides
  - PriorityQueue (and MinPriorityQueue) are LeetCode platform classes unavailable in Node.js; any oracle using them is permanent needs-oracle

---
## 2026-04-22 - US-033
- **What was implemented:**
  - Ran `python3 scripts/ralph/generate_tests.py --batch batch-020` — 17/20 generated (skip 0384/0385/0398)
  - Fixed 5 bugs discovered during batch-020:
    1. 0379 (PhoneDirectory): LRU check fired on `get` alone (no `put`). Fixed: changed LRU check to require BOTH `get` AND `put`. Added separate PhoneDirectory handler for `get+check+release`.
    2. 0384 (shuffle-an-array): Generic fallback passed no args to `Solution(nums[])`. Added shuffle handler `['shuffle', 'reset']` in `_build_design_inputs`. Still permanent needs-oracle (non-deterministic shuffle).
    3. 0386 (lexicographical-numbers): `lexicalOrder(n)` with n=1534236469 → tried to generate 1.5B numbers → OOM. Fix: slug override with small n.
    4. 0399 (evaluate-division): 3-param `(string[][], number[], string[][])` fell through to generic fallback. Fix: slug override.
    5. 0385/0398: NestedInteger return type and non-deterministic pick() are permanent needs-oracle.

- **Files changed:**
  - Added: 17 stubs (0362-0400 minus 3) in leetcode-playground/
  - Added: 17 tests in leetcode-playground/tests/
  - Modified: scripts/ralph/generate_tests.py (LRU check fix, slug overrides, new design handlers)
  - Modified: scripts/ralph/needs-oracle.json (3 permanent entries)

- **Learnings:**
  - LRU cache check must require BOTH `get` AND `put`; `get` alone matches too many design classes
  - Randomized/shuffle design classes are non-deterministic — cannot test with exact-equal even with correct constructor args; permanent needs-oracle
  - 3-param problems with mixed types `(string[][], number[], string[][])` need explicit slug overrides
  - Functions that return platform objects (NestedInteger) or use platform callbacks (guess) are permanent needs-oracle

---
