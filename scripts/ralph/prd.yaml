branchName: feature/populate-scenarios
userStories:
  - id: US-001
    title: Setup node project with jest test
    acceptanceCriteria:
      - setup node project with jest
      - create folder with leetcode-playground, it contain tests/ folder
      - 'each .js file inside leetcode-playground will have name format [number]-[problem-name].js e.g. 0002-add-two-numbers.js'
      - they contain function name exactly defined in leetcode, and exported.
      - tests/file containing same forrmat
      - create 2 leetcode file sample and tests first.
      - ensure jest test work correctly
    priority: 1
    passes: true
    notes: ""

  - id: US-002
    title: Patch prompt.md for this project (remove AGENTS.md + typecheck references)
    acceptanceCriteria:
      - Remove the AGENTS.md update step from scripts/ralph/prompt.md (file does not exist in this repo)
      - 'Replace any "Run typecheck and tests" step with "Run tests scoped to changed files via npx vitest run <paths>" (no typecheck script exists in this project)'
      - Ensure the prompt instructs ralph to append learnings to scripts/ralph/progress.txt
      - Keep all other steps intact
      - 'Commit the updated prompt with message: feat [US-002] - Patch prompt.md for this project'
    priority: 2
    passes: true
    notes: Must run first so subsequent iterations follow a prompt that matches this repo. Do not modify any other files.

  - id: US-003
    title: Rename 0042 typo (traping -> trapping)
    acceptanceCriteria:
      - Rename leetcode-playground/0042-traping-rain-water.js to leetcode-playground/0042-trapping-rain-water.js
      - Rename leetcode-playground/tests/0042-traping-rain-water.test.js to leetcode-playground/tests/0042-trapping-rain-water.test.js
      - Update the import path inside the renamed test file to point at ../0042-trapping-rain-water
      - Run npx vitest run leetcode-playground/tests/0042-trapping-rain-water.test.js and confirm the file loads without import/syntax errors (red assertions are acceptable)
      - 'Commit with message: feat [US-003] - Rename 0042 typo (traping -> trapping)'
    priority: 3
    passes: true
    notes: Must run before US-004 so the prep script sees the corrected filename when excluding already-implemented problems.

  - id: US-004
    title: Generate batch manifest for 3000-scenario target
    acceptanceCriteria:
      - Create scripts/ralph/build_batches.py that loads fetch-scrap-leetcode-problem/responseArchive/full_leetcode_problems_response.json
      - 'Script excludes problems already in leetcode-playground/ (match by 4-digit ID prefix from NNNN-*.js files); premium/paid problems are included'
      - Script sorts the remaining pool ascending by int(questionFrontendId)
      - Script caps the selection so existing + new lands within 3000 plus or minus 20 scenarios total
      - 'Script tags classHeavy problems (slug starts with design- OR title/slug contains any of: cache, stack, queue, heap, trie, iterator, serialize, randomized, median-finder, lfu, lru, union-find)'
      - Script enforces max 2 classHeavy problems per batch by overflowing extras into later batches
      - 'Script writes scripts/ralph/batches/batch-NNN.json (zero-padded 3 digits) with up to 20 problems each, schema {batchId, problems:[{id, slug, title, difficulty, tags, classHeavy}]}'
      - Script writes scripts/ralph/manifest.json index listing all batches in order with total problem count
      - Script is idempotent (safe to re-run; overwrites existing batch + manifest files)
      - Run python3 scripts/ralph/build_batches.py and verify scripts/ralph/batches/ contains batch-001.json .. batch-NNN.json plus manifest.json
      - 'Append one new userStory entry to scripts/ralph/prd.yaml per generated batch (US-005 .. US-(004+N)) with title "Implement batch-NNN", priority 4+N, passes false, and the batch-story acceptance criteria template defined in the notes field below'
      - 'Commit with message: feat [US-004] - Generate batch manifest for 3000-scenario target'
    priority: 4
    passes: true
    notes: |
      Prerequisite for all batch stories. Do NOT implement any LeetCode problems in this story - only the prep script + manifest + batch files + appended prd.yaml entries.

      Batch-story acceptance criteria template (use verbatim for each appended US-005..US-N entry, swapping NNN for the batch number):
        - For each problem in scripts/ralph/batches/batch-NNN.json:
        - Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)
        - Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }
        - Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)
        - For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution
        - Guard assertions that depend on a real return value with if (result !== undefined)
        - Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors
        - Commit with message "feat [US-0XX] - Implement batch-NNN" even if assertions are red
      Notes field for each appended entry: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-005
    title: "Implement batch-001"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-001.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-005] - Implement batch-001" even if assertions are red'
    priority: 5
    passes: true
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-006
    title: "Implement batch-002"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-002.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-006] - Implement batch-002" even if assertions are red'
    priority: 6
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-007
    title: "Implement batch-003"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-003.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-007] - Implement batch-003" even if assertions are red'
    priority: 7
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-008
    title: "Implement batch-004"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-004.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-008] - Implement batch-004" even if assertions are red'
    priority: 8
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-009
    title: "Implement batch-005"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-005.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-009] - Implement batch-005" even if assertions are red'
    priority: 9
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-010
    title: "Implement batch-006"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-006.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-010] - Implement batch-006" even if assertions are red'
    priority: 10
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-011
    title: "Implement batch-007"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-007.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-011] - Implement batch-007" even if assertions are red'
    priority: 11
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-012
    title: "Implement batch-008"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-008.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-012] - Implement batch-008" even if assertions are red'
    priority: 12
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-013
    title: "Implement batch-009"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-009.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-013] - Implement batch-009" even if assertions are red'
    priority: 13
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-014
    title: "Implement batch-010"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-010.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-014] - Implement batch-010" even if assertions are red'
    priority: 14
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-015
    title: "Implement batch-011"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-011.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-015] - Implement batch-011" even if assertions are red'
    priority: 15
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-016
    title: "Implement batch-012"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-012.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-016] - Implement batch-012" even if assertions are red'
    priority: 16
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-017
    title: "Implement batch-013"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-013.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-017] - Implement batch-013" even if assertions are red'
    priority: 17
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-018
    title: "Implement batch-014"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-014.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-018] - Implement batch-014" even if assertions are red'
    priority: 18
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-019
    title: "Implement batch-015"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-015.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-019] - Implement batch-015" even if assertions are red'
    priority: 19
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-020
    title: "Implement batch-016"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-016.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-020] - Implement batch-016" even if assertions are red'
    priority: 20
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-021
    title: "Implement batch-017"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-017.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-021] - Implement batch-017" even if assertions are red'
    priority: 21
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-022
    title: "Implement batch-018"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-018.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-022] - Implement batch-018" even if assertions are red'
    priority: 22
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-023
    title: "Implement batch-019"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-019.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-023] - Implement batch-019" even if assertions are red'
    priority: 23
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-024
    title: "Implement batch-020"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-020.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-024] - Implement batch-020" even if assertions are red'
    priority: 24
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-025
    title: "Implement batch-021"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-021.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-025] - Implement batch-021" even if assertions are red'
    priority: 25
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-026
    title: "Implement batch-022"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-022.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-026] - Implement batch-022" even if assertions are red'
    priority: 26
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-027
    title: "Implement batch-023"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-023.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-027] - Implement batch-023" even if assertions are red'
    priority: 27
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-028
    title: "Implement batch-024"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-024.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-028] - Implement batch-024" even if assertions are red'
    priority: 28
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-029
    title: "Implement batch-025"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-025.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-029] - Implement batch-025" even if assertions are red'
    priority: 29
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-030
    title: "Implement batch-026"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-026.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-030] - Implement batch-026" even if assertions are red'
    priority: 30
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-031
    title: "Implement batch-027"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-027.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-031] - Implement batch-027" even if assertions are red'
    priority: 31
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-032
    title: "Implement batch-028"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-028.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-032] - Implement batch-028" even if assertions are red'
    priority: 32
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-033
    title: "Implement batch-029"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-029.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-033] - Implement batch-029" even if assertions are red'
    priority: 33
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-034
    title: "Implement batch-030"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-030.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-034] - Implement batch-030" even if assertions are red'
    priority: 34
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-035
    title: "Implement batch-031"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-031.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-035] - Implement batch-031" even if assertions are red'
    priority: 35
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-036
    title: "Implement batch-032"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-032.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-036] - Implement batch-032" even if assertions are red'
    priority: 36
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-037
    title: "Implement batch-033"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-033.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-037] - Implement batch-033" even if assertions are red'
    priority: 37
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-038
    title: "Implement batch-034"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-034.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-038] - Implement batch-034" even if assertions are red'
    priority: 38
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-039
    title: "Implement batch-035"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-035.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-039] - Implement batch-035" even if assertions are red'
    priority: 39
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-040
    title: "Implement batch-036"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-036.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-040] - Implement batch-036" even if assertions are red'
    priority: 40
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-041
    title: "Implement batch-037"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-037.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-041] - Implement batch-037" even if assertions are red'
    priority: 41
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-042
    title: "Implement batch-038"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-038.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-042] - Implement batch-038" even if assertions are red'
    priority: 42
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-043
    title: "Implement batch-039"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-039.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-043] - Implement batch-039" even if assertions are red'
    priority: 43
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-044
    title: "Implement batch-040"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-040.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-044] - Implement batch-040" even if assertions are red'
    priority: 44
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-045
    title: "Implement batch-041"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-041.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-045] - Implement batch-041" even if assertions are red'
    priority: 45
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-046
    title: "Implement batch-042"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-042.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-046] - Implement batch-042" even if assertions are red'
    priority: 46
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-047
    title: "Implement batch-043"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-043.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-047] - Implement batch-043" even if assertions are red'
    priority: 47
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-048
    title: "Implement batch-044"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-044.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-048] - Implement batch-044" even if assertions are red'
    priority: 48
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-049
    title: "Implement batch-045"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-045.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-049] - Implement batch-045" even if assertions are red'
    priority: 49
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-050
    title: "Implement batch-046"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-046.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-050] - Implement batch-046" even if assertions are red'
    priority: 50
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-051
    title: "Implement batch-047"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-047.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-051] - Implement batch-047" even if assertions are red'
    priority: 51
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-052
    title: "Implement batch-048"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-048.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-052] - Implement batch-048" even if assertions are red'
    priority: 52
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-053
    title: "Implement batch-049"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-049.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-053] - Implement batch-049" even if assertions are red'
    priority: 53
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-054
    title: "Implement batch-050"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-050.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-054] - Implement batch-050" even if assertions are red'
    priority: 54
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-055
    title: "Implement batch-051"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-051.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-055] - Implement batch-051" even if assertions are red'
    priority: 55
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-056
    title: "Implement batch-052"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-052.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-056] - Implement batch-052" even if assertions are red'
    priority: 56
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-057
    title: "Implement batch-053"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-053.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-057] - Implement batch-053" even if assertions are red'
    priority: 57
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-058
    title: "Implement batch-054"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-054.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-058] - Implement batch-054" even if assertions are red'
    priority: 58
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-059
    title: "Implement batch-055"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-055.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-059] - Implement batch-055" even if assertions are red'
    priority: 59
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-060
    title: "Implement batch-056"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-056.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-060] - Implement batch-056" even if assertions are red'
    priority: 60
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-061
    title: "Implement batch-057"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-057.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-061] - Implement batch-057" even if assertions are red'
    priority: 61
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-062
    title: "Implement batch-058"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-058.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-062] - Implement batch-058" even if assertions are red'
    priority: 62
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-063
    title: "Implement batch-059"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-059.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-063] - Implement batch-059" even if assertions are red'
    priority: 63
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-064
    title: "Implement batch-060"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-060.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-064] - Implement batch-060" even if assertions are red'
    priority: 64
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-065
    title: "Implement batch-061"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-061.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-065] - Implement batch-061" even if assertions are red'
    priority: 65
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-066
    title: "Implement batch-062"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-062.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-066] - Implement batch-062" even if assertions are red'
    priority: 66
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-067
    title: "Implement batch-063"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-063.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-067] - Implement batch-063" even if assertions are red'
    priority: 67
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-068
    title: "Implement batch-064"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-064.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-068] - Implement batch-064" even if assertions are red'
    priority: 68
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-069
    title: "Implement batch-065"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-065.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-069] - Implement batch-065" even if assertions are red'
    priority: 69
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-070
    title: "Implement batch-066"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-066.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-070] - Implement batch-066" even if assertions are red'
    priority: 70
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-071
    title: "Implement batch-067"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-067.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-071] - Implement batch-067" even if assertions are red'
    priority: 71
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-072
    title: "Implement batch-068"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-068.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-072] - Implement batch-068" even if assertions are red'
    priority: 72
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-073
    title: "Implement batch-069"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-069.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-073] - Implement batch-069" even if assertions are red'
    priority: 73
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-074
    title: "Implement batch-070"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-070.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-074] - Implement batch-070" even if assertions are red'
    priority: 74
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-075
    title: "Implement batch-071"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-071.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-075] - Implement batch-071" even if assertions are red'
    priority: 75
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-076
    title: "Implement batch-072"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-072.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-076] - Implement batch-072" even if assertions are red'
    priority: 76
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-077
    title: "Implement batch-073"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-073.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-077] - Implement batch-073" even if assertions are red'
    priority: 77
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-078
    title: "Implement batch-074"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-074.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-078] - Implement batch-074" even if assertions are red'
    priority: 78
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-079
    title: "Implement batch-075"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-075.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-079] - Implement batch-075" even if assertions are red'
    priority: 79
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-080
    title: "Implement batch-076"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-076.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-080] - Implement batch-076" even if assertions are red'
    priority: 80
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-081
    title: "Implement batch-077"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-077.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-081] - Implement batch-077" even if assertions are red'
    priority: 81
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-082
    title: "Implement batch-078"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-078.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-082] - Implement batch-078" even if assertions are red'
    priority: 82
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-083
    title: "Implement batch-079"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-079.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-083] - Implement batch-079" even if assertions are red'
    priority: 83
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-084
    title: "Implement batch-080"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-080.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-084] - Implement batch-080" even if assertions are red'
    priority: 84
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-085
    title: "Implement batch-081"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-081.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-085] - Implement batch-081" even if assertions are red'
    priority: 85
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-086
    title: "Implement batch-082"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-082.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-086] - Implement batch-082" even if assertions are red'
    priority: 86
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-087
    title: "Implement batch-083"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-083.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-087] - Implement batch-083" even if assertions are red'
    priority: 87
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-088
    title: "Implement batch-084"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-084.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-088] - Implement batch-084" even if assertions are red'
    priority: 88
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-089
    title: "Implement batch-085"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-085.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-089] - Implement batch-085" even if assertions are red'
    priority: 89
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-090
    title: "Implement batch-086"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-086.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-090] - Implement batch-086" even if assertions are red'
    priority: 90
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-091
    title: "Implement batch-087"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-087.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-091] - Implement batch-087" even if assertions are red'
    priority: 91
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-092
    title: "Implement batch-088"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-088.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-092] - Implement batch-088" even if assertions are red'
    priority: 92
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-093
    title: "Implement batch-089"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-089.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-093] - Implement batch-089" even if assertions are red'
    priority: 93
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-094
    title: "Implement batch-090"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-090.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-094] - Implement batch-090" even if assertions are red'
    priority: 94
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-095
    title: "Implement batch-091"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-091.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-095] - Implement batch-091" even if assertions are red'
    priority: 95
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-096
    title: "Implement batch-092"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-092.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-096] - Implement batch-092" even if assertions are red'
    priority: 96
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-097
    title: "Implement batch-093"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-093.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-097] - Implement batch-093" even if assertions are red'
    priority: 97
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-098
    title: "Implement batch-094"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-094.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-098] - Implement batch-094" even if assertions are red'
    priority: 98
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-099
    title: "Implement batch-095"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-095.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-099] - Implement batch-095" even if assertions are red'
    priority: 99
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-100
    title: "Implement batch-096"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-096.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-100] - Implement batch-096" even if assertions are red'
    priority: 100
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-101
    title: "Implement batch-097"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-097.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-101] - Implement batch-097" even if assertions are red'
    priority: 101
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-102
    title: "Implement batch-098"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-098.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-102] - Implement batch-098" even if assertions are red'
    priority: 102
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-103
    title: "Implement batch-099"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-099.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-103] - Implement batch-099" even if assertions are red'
    priority: 103
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-104
    title: "Implement batch-100"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-100.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-104] - Implement batch-100" even if assertions are red'
    priority: 104
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-105
    title: "Implement batch-101"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-101.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-105] - Implement batch-101" even if assertions are red'
    priority: 105
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-106
    title: "Implement batch-102"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-102.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-106] - Implement batch-102" even if assertions are red'
    priority: 106
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-107
    title: "Implement batch-103"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-103.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-107] - Implement batch-103" even if assertions are red'
    priority: 107
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-108
    title: "Implement batch-104"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-104.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-108] - Implement batch-104" even if assertions are red'
    priority: 108
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-109
    title: "Implement batch-105"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-105.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-109] - Implement batch-105" even if assertions are red'
    priority: 109
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-110
    title: "Implement batch-106"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-106.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-110] - Implement batch-106" even if assertions are red'
    priority: 110
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-111
    title: "Implement batch-107"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-107.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-111] - Implement batch-107" even if assertions are red'
    priority: 111
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-112
    title: "Implement batch-108"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-108.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-112] - Implement batch-108" even if assertions are red'
    priority: 112
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-113
    title: "Implement batch-109"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-109.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-113] - Implement batch-109" even if assertions are red'
    priority: 113
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-114
    title: "Implement batch-110"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-110.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-114] - Implement batch-110" even if assertions are red'
    priority: 114
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-115
    title: "Implement batch-111"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-111.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-115] - Implement batch-111" even if assertions are red'
    priority: 115
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-116
    title: "Implement batch-112"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-112.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-116] - Implement batch-112" even if assertions are red'
    priority: 116
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-117
    title: "Implement batch-113"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-113.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-117] - Implement batch-113" even if assertions are red'
    priority: 117
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-118
    title: "Implement batch-114"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-114.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-118] - Implement batch-114" even if assertions are red'
    priority: 118
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-119
    title: "Implement batch-115"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-115.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-119] - Implement batch-115" even if assertions are red'
    priority: 119
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-120
    title: "Implement batch-116"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-116.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-120] - Implement batch-116" even if assertions are red'
    priority: 120
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-121
    title: "Implement batch-117"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-117.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-121] - Implement batch-117" even if assertions are red'
    priority: 121
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-122
    title: "Implement batch-118"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-118.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-122] - Implement batch-118" even if assertions are red'
    priority: 122
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-123
    title: "Implement batch-119"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-119.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-123] - Implement batch-119" even if assertions are red'
    priority: 123
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-124
    title: "Implement batch-120"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-120.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-124] - Implement batch-120" even if assertions are red'
    priority: 124
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-125
    title: "Implement batch-121"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-121.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-125] - Implement batch-121" even if assertions are red'
    priority: 125
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-126
    title: "Implement batch-122"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-122.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-126] - Implement batch-122" even if assertions are red'
    priority: 126
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-127
    title: "Implement batch-123"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-123.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-127] - Implement batch-123" even if assertions are red'
    priority: 127
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-128
    title: "Implement batch-124"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-124.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-128] - Implement batch-124" even if assertions are red'
    priority: 128
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-129
    title: "Implement batch-125"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-125.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-129] - Implement batch-125" even if assertions are red'
    priority: 129
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-130
    title: "Implement batch-126"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-126.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-130] - Implement batch-126" even if assertions are red'
    priority: 130
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-131
    title: "Implement batch-127"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-127.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-131] - Implement batch-127" even if assertions are red'
    priority: 131
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-132
    title: "Implement batch-128"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-128.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-132] - Implement batch-128" even if assertions are red'
    priority: 132
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-133
    title: "Implement batch-129"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-129.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-133] - Implement batch-129" even if assertions are red'
    priority: 133
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-134
    title: "Implement batch-130"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-130.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-134] - Implement batch-130" even if assertions are red'
    priority: 134
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-135
    title: "Implement batch-131"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-131.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-135] - Implement batch-131" even if assertions are red'
    priority: 135
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-136
    title: "Implement batch-132"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-132.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-136] - Implement batch-132" even if assertions are red'
    priority: 136
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-137
    title: "Implement batch-133"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-133.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-137] - Implement batch-133" even if assertions are red'
    priority: 137
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-138
    title: "Implement batch-134"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-134.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-138] - Implement batch-134" even if assertions are red'
    priority: 138
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-139
    title: "Implement batch-135"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-135.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-139] - Implement batch-135" even if assertions are red'
    priority: 139
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-140
    title: "Implement batch-136"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-136.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-140] - Implement batch-136" even if assertions are red'
    priority: 140
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-141
    title: "Implement batch-137"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-137.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-141] - Implement batch-137" even if assertions are red'
    priority: 141
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-142
    title: "Implement batch-138"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-138.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-142] - Implement batch-138" even if assertions are red'
    priority: 142
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-143
    title: "Implement batch-139"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-139.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-143] - Implement batch-139" even if assertions are red'
    priority: 143
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-144
    title: "Implement batch-140"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-140.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-144] - Implement batch-140" even if assertions are red'
    priority: 144
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-145
    title: "Implement batch-141"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-141.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-145] - Implement batch-141" even if assertions are red'
    priority: 145
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-146
    title: "Implement batch-142"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-142.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-146] - Implement batch-142" even if assertions are red'
    priority: 146
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-147
    title: "Implement batch-143"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-143.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-147] - Implement batch-143" even if assertions are red'
    priority: 147
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-148
    title: "Implement batch-144"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-144.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-148] - Implement batch-144" even if assertions are red'
    priority: 148
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."

  - id: US-149
    title: "Implement batch-145"
    acceptanceCriteria:
      - "For each problem in scripts/ralph/batches/batch-145.json:"
      - "Skip if leetcode-playground/<id>-<slug>.js already exists (idempotent resume)"
      - 'Create leetcode-playground/<id>-<slug>.js with JSDoc (1-2 sentence problem description + https://leetcode.com/problems/<slug>/description/ + @param + @return), named camelCase function, "// implement" body, and export { fn }'
      - "Create leetcode-playground/tests/<id>-<slug>.test.js with ES Module import from ../<id>-<slug>, describe('<id>-<slug>', ...), and 5-7 it() cases covering the adversarial edge cases from CLAUDE.md rule 5 (empty/min input, null/undefined, whitespace for strings, repetitive data, range-limit boundaries, off-by-one triggers)"
      - "For LinkedList/Tree/Graph problems, export the helper class (ListNode/TreeNode/GraphNode) alongside the solution"
      - "Guard assertions that depend on a real return value with if (result !== undefined)"
      - "Run only the new test files via npx vitest run leetcode-playground/tests/<id>-<slug>.test.js ... (do NOT run the full suite); files must load without import/syntax errors"
      - 'Commit with message "feat [US-149] - Implement batch-145" even if assertions are red'
    priority: 149
    passes: false
    notes: "Stubs intentionally fail assertions - red test output is the expected state and does NOT block the commit. Do not run the full test suite; scope to new files only."
