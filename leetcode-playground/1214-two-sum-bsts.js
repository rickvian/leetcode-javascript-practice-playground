/**
 * 1214. Two Sum BSTs https://leetcode.com/problems/two-sum-bsts/ Difficulty: Medium Given the roots of two binary search trees, root1 and root2, return true if and only if there is a node in the first tree and a node in the second tree whose values sum up to a given integer target.
 *
 * https://leetcode.com/problems/two-sum-bsts/description/
 *
 * @param {TreeNode} root1
 * @param {TreeNode} root2
 * @param {number} target
 * @return {boolean}
 */
var twoSumBSTs = function(root1, root2, target) {
    // implement
};

export { twoSumBSTs };
