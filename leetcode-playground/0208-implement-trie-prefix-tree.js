/**
 * 208. Implement Trie (Prefix Tree) https://leetcode.com/problems/implement-trie-prefix-tree/ Difficulty: Medium A trie (pronounced as "try") or prefix tree is a tree data structure used to efficiently store and retrieve keys in a dataset of strings.
 *
 * https://leetcode.com/problems/implement-trie-prefix-tree/description/
 */
var Trie = function() {
    // implement
};

Trie.prototype.insert = function(word) {
    // implement
};

Trie.prototype.search = function(word) {
    // implement
};

Trie.prototype.startsWith = function(prefix) {
    // implement
};

Trie.prototype.find = function(word) {
    // implement
};

export { Trie };
