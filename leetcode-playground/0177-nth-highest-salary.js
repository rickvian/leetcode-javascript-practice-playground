/**
 * SQL problem: return the nth distinct highest salary from the Employee table,
 * or null if fewer than n distinct salaries exist.
 *
 * https://leetcode.com/problems/nth-highest-salary/description/
 *
 * @param {number} n
 * @param {Array<{id:number,salary:number}>} employees
 * @return {number|null}
 */
var nthHighestSalary = function(n, employees) {
    // implement
};

export { nthHighestSalary }
