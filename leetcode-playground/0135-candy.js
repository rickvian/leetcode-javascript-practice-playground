/**
 * Given ratings of children standing in a line, return the minimum number of candies
 * needed so that each child has at least one and higher-rated kids get more than neighbors.
 *
 * https://leetcode.com/problems/candy/description/
 *
 * @param {number[]} ratings
 * @return {number}
 */
var candy = function(ratings) {
    // implement
};

export { candy }
