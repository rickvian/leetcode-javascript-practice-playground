/**
 * 306. Additive Number https://leetcode.com/problems/additive-number/ Difficulty: Medium An additive number is a string whose digits can form an additive sequence.
 *
 * https://leetcode.com/problems/additive-number/description/
 *
 * @param {string} num
 * @param {Array} group
 * @param {number} startIndex
 * @return {boolean}
 */
var isAdditiveNumber = function(num, group, startIndex) {
    // implement
};

export { isAdditiveNumber };
