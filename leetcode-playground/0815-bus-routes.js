/**
 * 815. Bus Routes https://leetcode.com/problems/bus-routes/ Difficulty: Hard You are given an array routes representing bus routes where routes[i] is a bus route that the ith bus repeats forever.
 *
 * https://leetcode.com/problems/bus-routes/description/
 *
 * @param {number[][]} routes
 * @param {number} source
 * @param {number} target
 * @return {number}
 */
var numBusesToDestination = function(routes, source, target) {
    // implement
};

export { numBusesToDestination };
