/**
 * Given an array where every element appears twice except for one, find and return that single element.
 *
 * https://leetcode.com/problems/single-number/description/
 *
 * @param {number[]} nums
 * @return {number}
 */
var singleNumber = function(nums) {
    // implement
};

export { singleNumber }
