/**
 * 484. Find Permutation https://leetcode.com/problems/find-permutation/ Difficulty: Medium A permutation perm of n integers of all the integers in the range [1, n] can be represented as a string s of length n - 1 where: - s[i] == 'I' if perm[i] < perm[i + 1], and - s[i] == 'D' if perm[i] > perm[i + 1].
 *
 * https://leetcode.com/problems/find-permutation/description/
 *
 * @param {string} s
 * @return {number[]}
 */
var findPermutation = function(s) {
    // implement
};

export { findPermutation };
