/**
 * Return the majority element of the array - the element appearing more than
 * floor(n/2) times (guaranteed to exist).
 *
 * https://leetcode.com/problems/majority-element/description/
 *
 * @param {number[]} nums
 * @return {number}
 */
var majorityElement = function(nums) {
    // implement
};

export { majorityElement }
