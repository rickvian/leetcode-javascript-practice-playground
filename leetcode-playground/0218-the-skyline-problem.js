/**
 * Given a list of buildings, return the skyline as a list of key [x, y]
 * points representing changes in height silhouette from left to right.
 *
 * https://leetcode.com/problems/the-skyline-problem/description/
 *
 * @param {number[][]} buildings - Array of [left, right, height] triples
 * @return {number[][]} Key points [x, y] sorted by x ascending
 */
var getSkyline = function(buildings) {
    // implement
};

export { getSkyline }
