/**
 * Given a signed 32-bit integer x, return x with its digits reversed, or 0 if the reversed value overflows.
 *
 * https://leetcode.com/problems/reverse-integer/description/
 *
 * @param {number} x
 * @return {number}
 */
var reverse = function(x) {
    // implement
};

export { reverse }
