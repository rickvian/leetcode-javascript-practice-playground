/**
 * Given an array nums and a target, return all unique quadruplets whose sum equals target.
 *
 * https://leetcode.com/problems/4sum/description/
 *
 * @param {number[]} nums
 * @param {number} target
 * @return {number[][]}
 */
var fourSum = function(nums, target) {
    // implement
};

export { fourSum }
