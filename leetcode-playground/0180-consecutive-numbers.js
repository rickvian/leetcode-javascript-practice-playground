/**
 * SQL problem: return all numbers that appear at least three times consecutively
 * (ordered by id) in the Logs table.
 *
 * https://leetcode.com/problems/consecutive-numbers/description/
 *
 * @param {Array<{id:number,num:(number|string)}>} logs
 * @return {Array<{ConsecutiveNums:(number|string)}>}
 */
var consecutiveNumbers = function(logs) {
    // implement
};

export { consecutiveNumbers }
