/**
 * Given a sorted array and a target value, return the index if found, else the index where it would be inserted.
 *
 * https://leetcode.com/problems/search-insert-position/description/
 *
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var searchInsert = function(nums, target) {
    // implement
};

export { searchInsert }
