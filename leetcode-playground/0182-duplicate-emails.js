/**
 * SQL problem: return all emails that appear more than once in the Person table.
 *
 * https://leetcode.com/problems/duplicate-emails/description/
 *
 * @param {Array<{id:number,email:string}>} persons
 * @return {Array<{Email:string}>}
 */
var duplicateEmails = function(persons) {
    // implement
};

export { duplicateEmails }
