/**
 * 166. Fraction to Recurring Decimal https://leetcode.com/problems/fraction-to-recurring-decimal/ Difficulty: Medium Given two integers representing the numerator and denominator of a fraction, return the fraction in string format.
 *
 * https://leetcode.com/problems/fraction-to-recurring-decimal/description/
 *
 * @param {number} numerator
 * @param {number} denominator
 * @return {string}
 */
var fractionToDecimal = function(numerator, denominator) {
    // implement
};

export { fractionToDecimal };
