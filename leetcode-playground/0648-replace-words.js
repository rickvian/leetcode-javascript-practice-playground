/**
 * 648. Replace Words https://leetcode.com/problems/replace-words/ Difficulty: Medium In English, we have a concept called root, which can be followed by some other words to form another longer word - let's call this word successor.
 *
 * https://leetcode.com/problems/replace-words/description/
 *
 * @param {string[]} dict
 * @param {string} sentence
 * @return {string}
 */
var replaceWords = function(dict, sentence) {
    // implement
};

export { replaceWords };
