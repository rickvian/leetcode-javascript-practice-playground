/**
 * Given two integers dividend and divisor, divide without using multiplication, division, or mod, clamped to int32.
 *
 * https://leetcode.com/problems/divide-two-integers/description/
 *
 * @param {number} dividend
 * @param {number} divisor
 * @return {number}
 */
var divide = function(dividend, divisor) {
    // implement
};

export { divide }
