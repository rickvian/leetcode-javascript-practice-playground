/**
 * 796. Rotate String https://leetcode.com/problems/rotate-string/ Difficulty: Easy Given two strings s and goal, return true if and only if s can become goal after some number of shifts on s.
 *
 * https://leetcode.com/problems/rotate-string/description/
 *
 * @param {string} s
 * @param {string} goal
 * @return {boolean}
 */
var rotateString = function(s, goal) {
    // implement
};

export { rotateString };
