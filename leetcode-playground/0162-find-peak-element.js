/**
 * Given an array where neighbors are strictly different, return the index of any
 * element strictly greater than both of its neighbors (edges are treated as -Infinity).
 *
 * https://leetcode.com/problems/find-peak-element/description/
 *
 * @param {number[]} nums
 * @return {number}
 */
var findPeakElement = function(nums) {
    // implement
};

export { findPeakElement }
