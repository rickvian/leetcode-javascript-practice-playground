/**
 * Given a text file, print just the 10th line of the file; modeled as a JS
 * function that takes the file contents and returns the 10th line (or "" if fewer).
 *
 * https://leetcode.com/problems/tenth-line/description/
 *
 * @param {string} text - File contents with lines separated by newlines
 * @return {string} The 10th line, or empty string when the file has fewer than 10 lines
 */
var tenthLine = function(text) {
    // implement
};

export { tenthLine }
