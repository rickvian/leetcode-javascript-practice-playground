/**
 * 839. Similar String Groups https://leetcode.com/problems/similar-string-groups/ Difficulty: Hard Two strings, X and Y, are considered similar if either they are identical or we can make them equivalent by swapping at most two letters (in distinct positions) within the string X.
 *
 * https://leetcode.com/problems/similar-string-groups/description/
 *
 * @param {string[]} strs
 * @return {number}
 */
var numSimilarGroups = function(strs) {
    // implement
};

export { numSimilarGroups };
