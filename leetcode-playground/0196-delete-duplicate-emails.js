/**
 * Delete all duplicate email entries keeping only the unique entry with the
 * smallest id; modeled as a JS function that returns the surviving rows.
 *
 * https://leetcode.com/problems/delete-duplicate-emails/description/
 *
 * @param {Array<{id: number, email: string}>} person - Person rows
 * @return {Array<{id: number, email: string}>} Surviving rows ordered by id ascending
 */
var deleteDuplicateEmails = function(person) {
    // implement
};

export { deleteDuplicateEmails }
