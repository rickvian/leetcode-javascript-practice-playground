/**
 * Given a collection of candidate numbers and a target, find all unique combinations where the candidates sum to target. Each candidate may be used once.
 *
 * https://leetcode.com/problems/combination-sum-ii/description/
 *
 * @param {number[]} candidates
 * @param {number} target
 * @return {number[][]}
 */
var combinationSum2 = function(candidates, target) {
    // implement
};

export { combinationSum2 }
