/**
 * Determine if two strings are exactly one edit (insert, delete, or replace) apart.
 *
 * https://leetcode.com/problems/one-edit-distance/description/
 *
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isOneEditDistance = function(s, t) {
    // implement
};

export { isOneEditDistance }
