/**
 * 663. Equal Tree Partition https://leetcode.com/problems/equal-tree-partition/ Difficulty: Medium Given the root of a binary tree, return true if you can partition the tree into two trees with equal sums of values after removing exactly one edge on the original tree.
 *
 * https://leetcode.com/problems/equal-tree-partition/description/
 *
 * @param {TreeNode} root
 * @return {boolean}
 */
var checkEqualTree = function(root) {
    // implement
};

export { checkEqualTree };
