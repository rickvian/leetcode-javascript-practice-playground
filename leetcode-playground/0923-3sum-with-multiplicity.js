/**
 * 923. 3Sum With Multiplicity https://leetcode.com/problems/3sum-with-multiplicity/ Difficulty: Medium Given an integer array arr, and an integer target, return the number of tuples i, j, k such that i < j < k and arr[i] + arr[j] + arr[k] == target.
 *
 * https://leetcode.com/problems/3sum-with-multiplicity/description/
 *
 * @param {number[]} arr
 * @param {number} target
 * @return {number}
 */
var threeSumMulti = function(arr, target) {
    // implement
};

export { threeSumMulti };
