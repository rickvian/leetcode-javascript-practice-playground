/**
 * Given an integer array and k, return true if there are two distinct indices
 * i and j such that nums[i] === nums[j] and abs(i - j) <= k.
 *
 * https://leetcode.com/problems/contains-duplicate-ii/description/
 *
 * @param {number[]} nums - Array of integers
 * @param {number} k - Maximum allowed index difference
 * @return {boolean} True if such a near-duplicate pair exists
 */
var containsNearbyDuplicate = function(nums, k) {
    // implement
};

export { containsNearbyDuplicate }
