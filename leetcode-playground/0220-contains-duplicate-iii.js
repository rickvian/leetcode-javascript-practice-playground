/**
 * Determine if the array has two distinct indices i,j with
 * abs(i - j) <= indexDiff and abs(nums[i] - nums[j]) <= valueDiff.
 *
 * https://leetcode.com/problems/contains-duplicate-iii/description/
 *
 * @param {number[]} nums - Array of integers
 * @param {number} indexDiff - Maximum allowed index gap
 * @param {number} valueDiff - Maximum allowed absolute value difference
 * @return {boolean} True if such a pair exists
 */
var containsNearbyAlmostDuplicate = function(nums, indexDiff, valueDiff) {
    // implement
};

export { containsNearbyAlmostDuplicate }
