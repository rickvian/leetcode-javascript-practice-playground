/**
 * SQL problem: for every person in the Person table, return firstName, lastName, city, state
 * (from the Address table if present) whether or not they have an address.
 *
 * https://leetcode.com/problems/combine-two-tables/description/
 *
 * @param {Array<{personId:number,firstName:string,lastName:string}>} persons
 * @param {Array<{addressId:number,personId:number,city:string,state:string}>} addresses
 * @return {Array<{firstName:string,lastName:string,city:(string|null),state:(string|null)}>}
 */
var combineTwoTables = function(persons, addresses) {
    // implement
};

export { combineTwoTables }
