/**
 * Return the maximum profit you can achieve from at most k buy/sell transactions
 * over the given daily prices (no simultaneous positions).
 *
 * https://leetcode.com/problems/best-time-to-buy-and-sell-stock-iv/description/
 *
 * @param {number} k
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function(k, prices) {
    // implement
};

export { maxProfit }
