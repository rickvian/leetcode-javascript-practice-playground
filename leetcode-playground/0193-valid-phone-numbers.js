/**
 * Shell problem reinterpreted as a function: given lines from a file, return only
 * those matching a valid US phone number in "(xxx) xxx-xxxx" or "xxx-xxx-xxxx" form.
 *
 * https://leetcode.com/problems/valid-phone-numbers/description/
 *
 * @param {string[]} lines
 * @return {string[]}
 */
var validPhoneNumbers = function(lines) {
    // implement
};

export { validPhoneNumbers }
