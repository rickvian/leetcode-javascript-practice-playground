/**
 * Given n and k, return the kth permutation sequence of the set [1, 2, ..., n] when all unique permutations are listed in lexicographic order.
 *
 * https://leetcode.com/problems/permutation-sequence/description/
 *
 * @param {number} n
 * @param {number} k
 * @return {string}
 */
var getPermutation = function(n, k) {
    // implement
};

export { getPermutation }
