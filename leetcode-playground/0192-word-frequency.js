/**
 * Shell problem reinterpreted as a function: given the full contents of a text file,
 * return each distinct word and its count, sorted by count descending then word ascending.
 *
 * https://leetcode.com/problems/word-frequency/description/
 *
 * @param {string} text
 * @return {string[]} Lines formatted as "<word> <count>"
 */
var wordFrequency = function(text) {
    // implement
};

export { wordFrequency }
