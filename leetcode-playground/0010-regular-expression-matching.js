/**
 * Given a string s and pattern p, implement regular expression matching with support for '.' and '*'.
 *
 * https://leetcode.com/problems/regular-expression-matching/description/
 *
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function(s, p) {
    // implement
};

export { isMatch }
