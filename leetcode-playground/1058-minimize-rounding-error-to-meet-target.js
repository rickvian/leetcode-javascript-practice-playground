/**
 * 1058. Minimize Rounding Error to Meet Target https://leetcode.com/problems/minimize-rounding-error-to-meet-target/ Difficulty: Medium Given an array of prices [p1,p2...,pn] and a target, round each price pi to Roundi(pi) so that the rounded array [Round1(p1),Round2(p2)...,Roundn(pn)] sums to the given target.
 *
 * https://leetcode.com/problems/minimize-rounding-error-to-meet-target/description/
 *
 * @param {string[]} prices
 * @param {number} target
 * @return {string}
 */
var minimizeError = function(prices, target) {
    // implement
};

export { minimizeError };
