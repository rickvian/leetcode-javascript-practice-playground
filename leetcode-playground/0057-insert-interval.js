/**
 * 57. Insert Interval https://leetcode.com/problems/insert-interval/ Difficulty: Medium You are given an array of non-overlapping intervals intervals where intervals[i] = [starti, endi] represent the start and the end of the ith interval and intervals is sorted in ascending order by starti.
 *
 * https://leetcode.com/problems/insert-interval/description/
 *
 * @param {number[][]} intervals
 * @param {number[]} newInterval
 * @return {number[][]}
 */
var insert = function(intervals, newInterval) {
    // implement
};

export { insert };
