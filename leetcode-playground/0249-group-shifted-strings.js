/**
 * Given a list of strings, group all strings that belong to the same shifting sequence (each character shifted by the same amount).
 *
 * https://leetcode.com/problems/group-shifted-strings/description/
 *
 * @param {string[]} strings
 * @return {string[][]}
 */
var groupStrings = function(strings) {
    // implement
};

export { groupStrings }
