/**
 * Given a string s, return true if s is a valid number per the rules covering
 * decimal, integer, signs, and optional exponent.
 *
 * https://leetcode.com/problems/valid-number/description/
 *
 * @param {string} s
 * @return {boolean}
 */
var isNumber = function(s) {
    // implement
};

export { isNumber }
