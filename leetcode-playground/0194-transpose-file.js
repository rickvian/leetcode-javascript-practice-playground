/**
 * Given a text file, transpose its content; modeled as a JS function that
 * takes the file contents as a string and returns the transposed string.
 *
 * https://leetcode.com/problems/transpose-file/description/
 *
 * @param {string} text - File contents with rows separated by newlines and columns separated by spaces
 * @return {string} Transposed file contents
 */
var transposeFile = function(text) {
    // implement
};

export { transposeFile }
