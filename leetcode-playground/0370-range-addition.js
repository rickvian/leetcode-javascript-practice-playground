/**
 * 370. Range Addition https://leetcode.com/problems/range-addition/ Difficulty: Medium You are given an integer length and an array updates where updates[i] = [startIdxi, endIdxi, inci].
 *
 * https://leetcode.com/problems/range-addition/description/
 *
 * @param {number} length
 * @param {number[][]} updates
 * @return {number[]}
 */
var getModifiedArray = function(length, updates) {
    // implement
};

export { getModifiedArray };
