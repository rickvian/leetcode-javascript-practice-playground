/**
 * 721. Accounts Merge https://leetcode.com/problems/accounts-merge/ Difficulty: Medium Given a list of accounts where each element accounts[i] is a list of strings, where the first element accounts[i][0] is a name, and the rest of the elements are emails representing emails of the account.
 *
 * https://leetcode.com/problems/accounts-merge/description/
 *
 * @param {string[][]} accounts
 * @return {string[][]}
 */
var accountsMerge = function(accounts) {
    // implement
};

export { accountsMerge };
