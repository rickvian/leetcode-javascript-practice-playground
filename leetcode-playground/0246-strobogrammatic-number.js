/**
 * Given a string num representing an integer, return true if num is a strobogrammatic number (looks the same when rotated 180 degrees).
 *
 * https://leetcode.com/problems/strobogrammatic-number/description/
 *
 * @param {string} num
 * @return {boolean}
 */
var isStrobogrammatic = function(num) {
    // implement
};

export { isStrobogrammatic }
