/**
 * Given an integer n, return all possible combinations of its factors (excluding 1 and n) such that their product equals n.
 *
 * https://leetcode.com/problems/factor-combinations/description/
 *
 * @param {number} n
 * @return {number[][]}
 */
var getFactors = function(n) {
    // implement
};

export { getFactors }
