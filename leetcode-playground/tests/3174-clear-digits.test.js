import { clearDigits } from '../3174-clear-digits.js';

describe('3174-clear-digits', () => {
    it('should handle basic case', () => {
        // expect(clearDigits(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(clearDigits(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(clearDigits(...)).toEqual(...);
    });
});
