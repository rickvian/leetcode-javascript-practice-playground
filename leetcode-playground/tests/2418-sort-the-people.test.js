import { sortThePeople } from '../2418-sort-the-people.js';

describe('2418-sort-the-people', () => {
    it('should handle basic case', () => {
        // expect(sortThePeople(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(sortThePeople(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(sortThePeople(...)).toEqual(...);
    });
});
