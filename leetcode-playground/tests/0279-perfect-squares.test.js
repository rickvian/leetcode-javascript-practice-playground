import { perfectSquares } from '../0279-perfect-squares.js';

describe('0279-perfect-squares', () => {
    it('should handle basic case', () => {
        // expect(perfectSquares(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(perfectSquares(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(perfectSquares(...)).toEqual(...);
    });
});
