import { largestValsFromLabels } from '../1090-largest-values-from-labels.js';

describe('1090-largest-values-from-labels', () => {
  it("largestValsFromLabels([])", () => {
    const result = largestValsFromLabels([]);
    expect(result).toEqual(0);
  });
});
