import { buddyStrings } from '../0859-buddy-strings.js';

describe('0859-buddy-strings', () => {
    it('should handle basic case', () => {
        // expect(buddyStrings(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(buddyStrings(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(buddyStrings(...)).toEqual(...);
    });
});
