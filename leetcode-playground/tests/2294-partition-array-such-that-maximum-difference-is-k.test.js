import { partitionArraySuchThatMaximumDifferenceIsK } from '../2294-partition-array-such-that-maximum-difference-is-k.js';
// TODO(backfill): replace with real LeetCode cases

describe('2294-partition-array-such-that-maximum-difference-is-k', () => {
    it('should handle basic case', () => {
        // expect(partitionArraySuchThatMaximumDifferenceIsK(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(partitionArraySuchThatMaximumDifferenceIsK(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(partitionArraySuchThatMaximumDifferenceIsK(...)).toEqual(...);
    });
});
