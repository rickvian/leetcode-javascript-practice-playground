import { wateringPlants } from '../2079-watering-plants.js';

describe('2079-watering-plants', () => {
    it('should handle basic case', () => {
        // expect(wateringPlants(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(wateringPlants(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(wateringPlants(...)).toEqual(...);
    });
});
