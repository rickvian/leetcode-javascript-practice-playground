import { findIndicesWithIndexAndValueDifferenceI } from '../2903-find-indices-with-index-and-value-difference-i.js';
// TODO(backfill): replace with real LeetCode cases

describe('2903-find-indices-with-index-and-value-difference-i', () => {
    it('should handle basic case', () => {
        // expect(findIndicesWithIndexAndValueDifferenceI(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(findIndicesWithIndexAndValueDifferenceI(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(findIndicesWithIndexAndValueDifferenceI(...)).toEqual(...);
    });
});
