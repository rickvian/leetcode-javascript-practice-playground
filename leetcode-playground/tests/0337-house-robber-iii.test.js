import { houseRobberIii } from '../0337-house-robber-iii.js';

describe('0337-house-robber-iii', () => {
    it('should handle basic case', () => {
        // expect(houseRobberIii(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(houseRobberIii(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(houseRobberIii(...)).toEqual(...);
    });
});
