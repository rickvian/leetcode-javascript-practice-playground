import { findTheMinimumNumberOfFibonacciNumbersWhoseSumIsK } from '../1414-find-the-minimum-number-of-fibonacci-numbers-whose-sum-is-k.js';
// TODO(backfill): replace with real LeetCode cases

describe('1414-find-the-minimum-number-of-fibonacci-numbers-whose-sum-is-k', () => {
    it('should handle basic case', () => {
        // expect(findTheMinimumNumberOfFibonacciNumbersWhoseSumIsK(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(findTheMinimumNumberOfFibonacciNumbersWhoseSumIsK(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(findTheMinimumNumberOfFibonacciNumbersWhoseSumIsK(...)).toEqual(...);
    });
});
