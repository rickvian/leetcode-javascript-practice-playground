import { partitionList } from '../0086-partition-list.js';

describe('0086-partition-list', () => {
    it('should handle basic case', () => {
        // expect(partitionList(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(partitionList(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(partitionList(...)).toEqual(...);
    });
});
