import { harshadNumber } from '../3099-harshad-number.js';

describe('3099-harshad-number', () => {
    it('should handle basic case', () => {
        // expect(harshadNumber(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(harshadNumber(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(harshadNumber(...)).toEqual(...);
    });
});
