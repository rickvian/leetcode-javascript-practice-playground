import { soupServings } from '../0808-soup-servings.js';

describe('0808-soup-servings', () => {
    it('should handle basic case', () => {
        // expect(soupServings(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(soupServings(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(soupServings(...)).toEqual(...);
    });
});
