import { catAndMouseIi } from '../1728-cat-and-mouse-ii.js';

describe('1728-cat-and-mouse-ii', () => {
    it('should handle basic case', () => {
        // expect(catAndMouseIi(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(catAndMouseIi(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(catAndMouseIi(...)).toEqual(...);
    });
});
