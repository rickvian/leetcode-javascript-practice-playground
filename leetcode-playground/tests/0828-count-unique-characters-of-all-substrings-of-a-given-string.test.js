import { countUniqueCharactersOfAllSubstringsOfAGivenString } from '../0828-count-unique-characters-of-all-substrings-of-a-given-string.js';
// TODO(backfill): replace with real LeetCode cases

describe('0828-count-unique-characters-of-all-substrings-of-a-given-string', () => {
    it('should handle basic case', () => {
        // expect(countUniqueCharactersOfAllSubstringsOfAGivenString(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(countUniqueCharactersOfAllSubstringsOfAGivenString(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(countUniqueCharactersOfAllSubstringsOfAGivenString(...)).toEqual(...);
    });
});
