import { maximumScoreFromRemovingSubstrings } from '../1717-maximum-score-from-removing-substrings.js';

describe('1717-maximum-score-from-removing-substrings', () => {
    it('should handle basic case', () => {
        // expect(maximumScoreFromRemovingSubstrings(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(maximumScoreFromRemovingSubstrings(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(maximumScoreFromRemovingSubstrings(...)).toEqual(...);
    });
});
