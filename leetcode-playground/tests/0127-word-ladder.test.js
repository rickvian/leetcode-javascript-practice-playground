import { wordLadder } from '../0127-word-ladder.js';

describe('0127-word-ladder', () => {
    it('should handle basic case', () => {
        // expect(wordLadder(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(wordLadder(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(wordLadder(...)).toEqual(...);
    });
});
