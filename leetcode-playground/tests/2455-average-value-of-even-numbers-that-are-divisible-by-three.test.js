import { averageValueOfEvenNumbersThatAreDivisibleByThree } from '../2455-average-value-of-even-numbers-that-are-divisible-by-three.js';

describe('2455-average-value-of-even-numbers-that-are-divisible-by-three', () => {
    it('should handle basic case', () => {
        // expect(averageValueOfEvenNumbersThatAreDivisibleByThree(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(averageValueOfEvenNumbersThatAreDivisibleByThree(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(averageValueOfEvenNumbersThatAreDivisibleByThree(...)).toEqual(...);
    });
});
