import { onlineElection } from '../0911-online-election.js';

describe('0911-online-election', () => {
    it('should handle basic case', () => {
        // expect(onlineElection(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(onlineElection(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(onlineElection(...)).toEqual(...);
    });
});
