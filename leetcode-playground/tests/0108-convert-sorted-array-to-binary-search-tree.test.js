import { convertSortedArrayToBinarySearchTree } from '../0108-convert-sorted-array-to-binary-search-tree.js';
// TODO(backfill): replace with real LeetCode cases

describe('0108-convert-sorted-array-to-binary-search-tree', () => {
    it('should handle basic case', () => {
        // expect(convertSortedArrayToBinarySearchTree(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(convertSortedArrayToBinarySearchTree(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(convertSortedArrayToBinarySearchTree(...)).toEqual(...);
    });
});
