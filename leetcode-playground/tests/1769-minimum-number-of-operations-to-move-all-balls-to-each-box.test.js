import { minimumNumberOfOperationsToMoveAllBallsToEachBox } from '../1769-minimum-number-of-operations-to-move-all-balls-to-each-box.js';
// TODO(backfill): replace with real LeetCode cases

describe('1769-minimum-number-of-operations-to-move-all-balls-to-each-box', () => {
    it('should handle basic case', () => {
        // expect(minimumNumberOfOperationsToMoveAllBallsToEachBox(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(minimumNumberOfOperationsToMoveAllBallsToEachBox(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(minimumNumberOfOperationsToMoveAllBallsToEachBox(...)).toEqual(...);
    });
});
