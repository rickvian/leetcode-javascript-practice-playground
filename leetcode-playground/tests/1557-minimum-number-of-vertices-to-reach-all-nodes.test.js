import { minimumNumberOfVerticesToReachAllNodes } from '../1557-minimum-number-of-vertices-to-reach-all-nodes.js';

describe('1557-minimum-number-of-vertices-to-reach-all-nodes', () => {
    it('should handle basic case', () => {
        // expect(minimumNumberOfVerticesToReachAllNodes(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(minimumNumberOfVerticesToReachAllNodes(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(minimumNumberOfVerticesToReachAllNodes(...)).toEqual(...);
    });
});
