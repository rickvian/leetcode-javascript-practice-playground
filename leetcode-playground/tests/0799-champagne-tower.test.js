import { champagneTower } from '../0799-champagne-tower.js';

describe('0799-champagne-tower', () => {
    it('should handle basic case', () => {
        // expect(champagneTower(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(champagneTower(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(champagneTower(...)).toEqual(...);
    });
});
