import { selfCrossing } from '../0335-self-crossing.js';

describe('0335-self-crossing', () => {
    it('should handle basic case', () => {
        // expect(selfCrossing(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(selfCrossing(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(selfCrossing(...)).toEqual(...);
    });
});
