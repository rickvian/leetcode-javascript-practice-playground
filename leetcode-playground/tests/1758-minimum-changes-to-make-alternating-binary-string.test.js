import { minimumChangesToMakeAlternatingBinaryString } from '../1758-minimum-changes-to-make-alternating-binary-string.js';
// TODO(backfill): replace with real LeetCode cases

describe('1758-minimum-changes-to-make-alternating-binary-string', () => {
    it('should handle basic case', () => {
        // expect(minimumChangesToMakeAlternatingBinaryString(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(minimumChangesToMakeAlternatingBinaryString(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(minimumChangesToMakeAlternatingBinaryString(...)).toEqual(...);
    });
});
