import { maximalSquare } from '../0221-maximal-square.js';

describe('0221-maximal-square', () => {
    it('should handle basic case', () => {
        // expect(maximalSquare(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(maximalSquare(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(maximalSquare(...)).toEqual(...);
    });
});
