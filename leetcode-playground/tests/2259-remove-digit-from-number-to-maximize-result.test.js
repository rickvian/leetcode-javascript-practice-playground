import { removeDigitFromNumberToMaximizeResult } from '../2259-remove-digit-from-number-to-maximize-result.js';
// TODO(backfill): replace with real LeetCode cases

describe('2259-remove-digit-from-number-to-maximize-result', () => {
    it('should handle basic case', () => {
        // expect(removeDigitFromNumberToMaximizeResult(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(removeDigitFromNumberToMaximizeResult(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(removeDigitFromNumberToMaximizeResult(...)).toEqual(...);
    });
});
