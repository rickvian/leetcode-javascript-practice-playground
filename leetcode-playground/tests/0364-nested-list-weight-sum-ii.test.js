import { depthSumInverse } from '../0364-nested-list-weight-sum-ii.js';

describe('0364-nested-list-weight-sum-ii', () => {
  it("depthSumInverse([])", () => {
    const result = depthSumInverse([]);
    expect(result).toEqual(0);
  });
});
