import { maximumPointsInAnArcheryCompetition } from '../2212-maximum-points-in-an-archery-competition.js';

describe('2212-maximum-points-in-an-archery-competition', () => {
    it('should handle basic case', () => {
        // expect(maximumPointsInAnArcheryCompetition(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(maximumPointsInAnArcheryCompetition(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(maximumPointsInAnArcheryCompetition(...)).toEqual(...);
    });
});
