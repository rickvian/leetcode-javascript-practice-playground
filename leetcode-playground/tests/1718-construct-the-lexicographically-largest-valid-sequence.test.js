import { constructTheLexicographicallyLargestValidSequence } from '../1718-construct-the-lexicographically-largest-valid-sequence.js';

describe('1718-construct-the-lexicographically-largest-valid-sequence', () => {
    it('should handle basic case', () => {
        // expect(constructTheLexicographicallyLargestValidSequence(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(constructTheLexicographicallyLargestValidSequence(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(constructTheLexicographicallyLargestValidSequence(...)).toEqual(...);
    });
});
