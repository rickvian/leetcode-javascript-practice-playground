import { countSubtreesWithMaxDistanceBetweenCities } from '../1617-count-subtrees-with-max-distance-between-cities.js';

describe('1617-count-subtrees-with-max-distance-between-cities', () => {
    it('should handle basic case', () => {
        // expect(countSubtreesWithMaxDistanceBetweenCities(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(countSubtreesWithMaxDistanceBetweenCities(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(countSubtreesWithMaxDistanceBetweenCities(...)).toEqual(...);
    });
});
