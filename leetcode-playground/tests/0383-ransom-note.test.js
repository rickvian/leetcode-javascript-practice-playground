import { ransomNote } from '../0383-ransom-note.js';

describe('0383-ransom-note', () => {
    it('should handle basic case', () => {
        // expect(ransomNote(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(ransomNote(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(ransomNote(...)).toEqual(...);
    });
});
