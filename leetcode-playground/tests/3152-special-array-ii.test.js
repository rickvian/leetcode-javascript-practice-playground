import { specialArrayIi } from '../3152-special-array-ii.js';

describe('3152-special-array-ii', () => {
    it('should handle basic case', () => {
        // expect(specialArrayIi(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(specialArrayIi(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(specialArrayIi(...)).toEqual(...);
    });
});
