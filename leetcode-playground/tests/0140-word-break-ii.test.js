import { wordBreak } from '../0140-word-break-ii.js';

describe('0140-word-break-ii', () => {
  it("wordBreak([])", () => {
    const result = wordBreak([]);
    expect(result).toEqual([""]);
  });
});
