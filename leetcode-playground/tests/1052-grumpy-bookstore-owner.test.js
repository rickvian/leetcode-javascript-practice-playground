import { maxSatisfied } from '../1052-grumpy-bookstore-owner.js';

describe('1052-grumpy-bookstore-owner', () => {
  it("maxSatisfied([])", () => {
    const result = maxSatisfied([]);
    expect(result).toEqual(0);
  });
});
