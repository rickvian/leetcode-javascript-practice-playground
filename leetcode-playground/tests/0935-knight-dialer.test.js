import { knightDialer } from '../0935-knight-dialer.js';

describe('0935-knight-dialer', () => {
    it('should handle basic case', () => {
        // expect(knightDialer(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(knightDialer(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(knightDialer(...)).toEqual(...);
    });
});
