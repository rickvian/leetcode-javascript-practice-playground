import { maximumGap } from '../0164-maximum-gap.js';

describe('0164-maximum-gap', () => {
    it('should handle basic case', () => {
        // expect(maximumGap(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(maximumGap(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(maximumGap(...)).toEqual(...);
    });
});
