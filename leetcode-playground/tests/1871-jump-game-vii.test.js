import { jumpGameVii } from '../1871-jump-game-vii.js';

describe('1871-jump-game-vii', () => {
    it('should handle basic case', () => {
        // expect(jumpGameVii(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(jumpGameVii(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(jumpGameVii(...)).toEqual(...);
    });
});
