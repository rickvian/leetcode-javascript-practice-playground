import { binaryWatch } from '../0401-binary-watch.js';

describe('0401-binary-watch', () => {
    it('should handle basic case', () => {
        // expect(binaryWatch(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(binaryWatch(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(binaryWatch(...)).toEqual(...);
    });
});
