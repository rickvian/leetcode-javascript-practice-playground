import { twoOutOfThree } from '../2032-two-out-of-three.js';

describe('2032-two-out-of-three', () => {
    it('should handle basic case', () => {
        // expect(twoOutOfThree(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(twoOutOfThree(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(twoOutOfThree(...)).toEqual(...);
    });
});
