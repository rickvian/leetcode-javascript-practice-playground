import { monotonicArray } from '../0896-monotonic-array.js';

describe('0896-monotonic-array', () => {
    it('should handle basic case', () => {
        // expect(monotonicArray(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(monotonicArray(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(monotonicArray(...)).toEqual(...);
    });
});
