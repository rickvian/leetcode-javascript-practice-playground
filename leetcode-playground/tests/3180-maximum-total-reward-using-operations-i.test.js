import { maximumTotalRewardUsingOperationsI } from '../3180-maximum-total-reward-using-operations-i.js';

describe('3180-maximum-total-reward-using-operations-i', () => {
    it('should handle basic case', () => {
        // expect(maximumTotalRewardUsingOperationsI(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(maximumTotalRewardUsingOperationsI(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(maximumTotalRewardUsingOperationsI(...)).toEqual(...);
    });
});
