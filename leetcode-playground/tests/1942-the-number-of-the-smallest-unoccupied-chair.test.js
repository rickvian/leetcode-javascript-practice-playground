import { theNumberOfTheSmallestUnoccupiedChair } from '../1942-the-number-of-the-smallest-unoccupied-chair.js';

describe('1942-the-number-of-the-smallest-unoccupied-chair', () => {
    it('should handle basic case', () => {
        // expect(theNumberOfTheSmallestUnoccupiedChair(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(theNumberOfTheSmallestUnoccupiedChair(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(theNumberOfTheSmallestUnoccupiedChair(...)).toEqual(...);
    });
});
