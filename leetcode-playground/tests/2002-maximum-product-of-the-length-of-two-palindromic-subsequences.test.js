import { maximumProductOfTheLengthOfTwoPalindromicSubsequences } from '../2002-maximum-product-of-the-length-of-two-palindromic-subsequences.js';
// TODO(backfill): replace with real LeetCode cases

describe('2002-maximum-product-of-the-length-of-two-palindromic-subsequences', () => {
    it('should handle basic case', () => {
        // expect(maximumProductOfTheLengthOfTwoPalindromicSubsequences(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(maximumProductOfTheLengthOfTwoPalindromicSubsequences(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(maximumProductOfTheLengthOfTwoPalindromicSubsequences(...)).toEqual(...);
    });
});
