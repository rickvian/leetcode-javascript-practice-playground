import { shuffleAnArray } from '../0384-shuffle-an-array.js';

describe('0384-shuffle-an-array', () => {
    it('should handle basic case', () => {
        // expect(shuffleAnArray(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(shuffleAnArray(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(shuffleAnArray(...)).toEqual(...);
    });
});
