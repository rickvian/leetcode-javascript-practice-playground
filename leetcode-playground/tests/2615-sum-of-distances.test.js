import { sumOfDistances } from '../2615-sum-of-distances.js';

describe('2615-sum-of-distances', () => {
    it('should handle basic case', () => {
        // expect(sumOfDistances(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(sumOfDistances(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(sumOfDistances(...)).toEqual(...);
    });
});
