import { permutationsIi } from '../0047-permutations-ii.js';

describe('0047-permutations-ii', () => {
    it('should handle basic case', () => {
        // expect(permutationsIi(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(permutationsIi(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(permutationsIi(...)).toEqual(...);
    });
});
