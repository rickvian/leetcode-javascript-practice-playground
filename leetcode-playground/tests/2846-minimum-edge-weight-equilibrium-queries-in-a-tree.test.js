import { minimumEdgeWeightEquilibriumQueriesInATree } from '../2846-minimum-edge-weight-equilibrium-queries-in-a-tree.js';

describe('2846-minimum-edge-weight-equilibrium-queries-in-a-tree', () => {
    it('should handle basic case', () => {
        // expect(minimumEdgeWeightEquilibriumQueriesInATree(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(minimumEdgeWeightEquilibriumQueriesInATree(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(minimumEdgeWeightEquilibriumQueriesInATree(...)).toEqual(...);
    });
});
