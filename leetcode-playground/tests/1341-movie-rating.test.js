import { movieRating } from '../1341-movie-rating.js';

describe('1341-movie-rating', () => {
    it('should handle basic case', () => {
        // expect(movieRating(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(movieRating(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(movieRating(...)).toEqual(...);
    });
});
