import { bigCountries } from '../0595-big-countries.js';

describe('0595-big-countries', () => {
    it('should handle basic case', () => {
        // expect(bigCountries(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(bigCountries(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(bigCountries(...)).toEqual(...);
    });
});
