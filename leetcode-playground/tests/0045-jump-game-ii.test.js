import { jumpGameIi } from '../0045-jump-game-ii.js';

describe('0045-jump-game-ii', () => {
    it('should handle basic case', () => {
        // expect(jumpGameIi(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(jumpGameIi(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(jumpGameIi(...)).toEqual(...);
    });
});
