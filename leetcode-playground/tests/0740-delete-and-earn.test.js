import { deleteAndEarn } from '../0740-delete-and-earn.js';

describe('0740-delete-and-earn', () => {
    it('should handle basic case', () => {
        // expect(deleteAndEarn(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(deleteAndEarn(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(deleteAndEarn(...)).toEqual(...);
    });
});
