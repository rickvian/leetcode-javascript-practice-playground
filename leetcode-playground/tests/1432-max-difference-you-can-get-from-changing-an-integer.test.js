import { maxDifferenceYouCanGetFromChangingAnInteger } from '../1432-max-difference-you-can-get-from-changing-an-integer.js';

describe('1432-max-difference-you-can-get-from-changing-an-integer', () => {
    it('should handle basic case', () => {
        // expect(maxDifferenceYouCanGetFromChangingAnInteger(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(maxDifferenceYouCanGetFromChangingAnInteger(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(maxDifferenceYouCanGetFromChangingAnInteger(...)).toEqual(...);
    });
});
