import { permutationsIv } from '../3470-permutations-iv.js';

describe('3470-permutations-iv', () => {
    it('should handle basic case', () => {
        // expect(permutationsIv(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(permutationsIv(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(permutationsIv(...)).toEqual(...);
    });
});
