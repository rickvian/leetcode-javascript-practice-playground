import { numberOfLongestIncreasingSubsequence } from '../0673-number-of-longest-increasing-subsequence.js';

describe('0673-number-of-longest-increasing-subsequence', () => {
    it('should handle basic case', () => {
        // expect(numberOfLongestIncreasingSubsequence(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(numberOfLongestIncreasingSubsequence(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(numberOfLongestIncreasingSubsequence(...)).toEqual(...);
    });
});
