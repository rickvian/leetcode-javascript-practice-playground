import { findChampionI } from '../2923-find-champion-i.js';

describe('2923-find-champion-i', () => {
    it('should handle basic case', () => {
        // expect(findChampionI(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(findChampionI(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(findChampionI(...)).toEqual(...);
    });
});
