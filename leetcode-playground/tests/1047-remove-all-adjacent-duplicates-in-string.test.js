import { removeAllAdjacentDuplicatesInString } from '../1047-remove-all-adjacent-duplicates-in-string.js';
// TODO(backfill): replace with real LeetCode cases

describe('1047-remove-all-adjacent-duplicates-in-string', () => {
    it('should handle basic case', () => {
        // expect(removeAllAdjacentDuplicatesInString(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(removeAllAdjacentDuplicatesInString(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(removeAllAdjacentDuplicatesInString(...)).toEqual(...);
    });
});
