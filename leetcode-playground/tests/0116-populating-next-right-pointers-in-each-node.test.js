import { populatingNextRightPointersInEachNode } from '../0116-populating-next-right-pointers-in-each-node.js';

describe('0116-populating-next-right-pointers-in-each-node', () => {
    it('should handle basic case', () => {
        // expect(populatingNextRightPointersInEachNode(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(populatingNextRightPointersInEachNode(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(populatingNextRightPointersInEachNode(...)).toEqual(...);
    });
});
