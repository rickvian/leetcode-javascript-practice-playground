import { findTheDistanceValue } from '../1385-find-the-distance-value-between-two-arrays.js';

describe('1385-find-the-distance-value-between-two-arrays', () => {
  it("findTheDistanceValue([])", () => {
    const result = findTheDistanceValue([]);
    expect(result).toEqual(0);
  });
});
