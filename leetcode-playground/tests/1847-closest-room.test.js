import { closestRoom } from '../1847-closest-room.js';

describe('1847-closest-room', () => {
    it('should handle basic case', () => {
        // expect(closestRoom(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(closestRoom(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(closestRoom(...)).toEqual(...);
    });
});
