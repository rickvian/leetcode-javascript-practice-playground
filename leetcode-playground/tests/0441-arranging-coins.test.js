import { arrangingCoins } from '../0441-arranging-coins.js';

describe('0441-arranging-coins', () => {
    it('should handle basic case', () => {
        // expect(arrangingCoins(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(arrangingCoins(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(arrangingCoins(...)).toEqual(...);
    });
});
