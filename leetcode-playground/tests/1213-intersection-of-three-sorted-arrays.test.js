import { arraysIntersection } from '../1213-intersection-of-three-sorted-arrays.js';

describe('1213-intersection-of-three-sorted-arrays', () => {
  it("arraysIntersection([])", () => {
    const result = arraysIntersection([]);
    expect(result).toEqual([]);
  });
});
