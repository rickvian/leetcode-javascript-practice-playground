import { randomPickWithBlacklist } from '../0710-random-pick-with-blacklist.js';

describe('0710-random-pick-with-blacklist', () => {
    it('should handle basic case', () => {
        // expect(randomPickWithBlacklist(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(randomPickWithBlacklist(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(randomPickWithBlacklist(...)).toEqual(...);
    });
});
