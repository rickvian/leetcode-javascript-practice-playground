import { rectangleAreaIi } from '../0850-rectangle-area-ii.js';

describe('0850-rectangle-area-ii', () => {
    it('should handle basic case', () => {
        // expect(rectangleAreaIi(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(rectangleAreaIi(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(rectangleAreaIi(...)).toEqual(...);
    });
});
