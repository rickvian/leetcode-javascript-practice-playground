import { minimumAmountOfTimeToCollectGarbage } from '../2391-minimum-amount-of-time-to-collect-garbage.js';
// TODO(backfill): replace with real LeetCode cases

describe('2391-minimum-amount-of-time-to-collect-garbage', () => {
    it('should handle basic case', () => {
        // expect(minimumAmountOfTimeToCollectGarbage(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(minimumAmountOfTimeToCollectGarbage(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(minimumAmountOfTimeToCollectGarbage(...)).toEqual(...);
    });
});
