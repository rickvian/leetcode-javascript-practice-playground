import { findFirstAndLastPositionOfElementInSortedArray } from '../0034-find-first-and-last-position-of-element-in-sorted-array.js';

describe('0034-find-first-and-last-position-of-element-in-sorted-array', () => {
    it('should handle basic case', () => {
        // expect(findFirstAndLastPositionOfElementInSortedArray(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(findFirstAndLastPositionOfElementInSortedArray(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(findFirstAndLastPositionOfElementInSortedArray(...)).toEqual(...);
    });
});
