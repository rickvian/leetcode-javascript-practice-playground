import { miceAndCheese } from '../2611-mice-and-cheese.js';

describe('2611-mice-and-cheese', () => {
    it('should handle basic case', () => {
        // expect(miceAndCheese(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(miceAndCheese(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(miceAndCheese(...)).toEqual(...);
    });
});
