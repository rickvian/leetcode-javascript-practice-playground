import { findBooksWithNoAvailableCopies } from '../3570-find-books-with-no-available-copies.js';
// TODO(backfill): replace with real LeetCode cases

describe('3570-find-books-with-no-available-copies', () => {
    it('should handle basic case', () => {
        // expect(findBooksWithNoAvailableCopies(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(findBooksWithNoAvailableCopies(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(findBooksWithNoAvailableCopies(...)).toEqual(...);
    });
});
