import { gasStation } from '../0134-gas-station.js';

describe('0134-gas-station', () => {
    it('should handle basic case', () => {
        // expect(gasStation(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(gasStation(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(gasStation(...)).toEqual(...);
    });
});
