import { editDistance } from '../0072-edit-distance.js';

describe('0072-edit-distance', () => {
    it('should handle basic case', () => {
        // expect(editDistance(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(editDistance(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(editDistance(...)).toEqual(...);
    });
});
