import { maximumNumberOfEventsThatCanBeAttendedIi } from '../1751-maximum-number-of-events-that-can-be-attended-ii.js';

describe('1751-maximum-number-of-events-that-can-be-attended-ii', () => {
    it('should handle basic case', () => {
        // expect(maximumNumberOfEventsThatCanBeAttendedIi(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(maximumNumberOfEventsThatCanBeAttendedIi(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(maximumNumberOfEventsThatCanBeAttendedIi(...)).toEqual(...);
    });
});
