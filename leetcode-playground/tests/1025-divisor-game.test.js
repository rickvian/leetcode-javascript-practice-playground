import { divisorGame } from '../1025-divisor-game.js';

describe('1025-divisor-game', () => {
    it('should handle basic case', () => {
        // expect(divisorGame(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(divisorGame(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(divisorGame(...)).toEqual(...);
    });
});
