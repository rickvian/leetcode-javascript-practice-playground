import { lengthOfLongestSubarrayWithAtMostKFrequency } from '../2958-length-of-longest-subarray-with-at-most-k-frequency.js';
// TODO(backfill): replace with real LeetCode cases

describe('2958-length-of-longest-subarray-with-at-most-k-frequency', () => {
    it('should handle basic case', () => {
        // expect(lengthOfLongestSubarrayWithAtMostKFrequency(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(lengthOfLongestSubarrayWithAtMostKFrequency(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(lengthOfLongestSubarrayWithAtMostKFrequency(...)).toEqual(...);
    });
});
