import { minimumNumberOfStepsToMakeTwoStringsAnagram } from '../1347-minimum-number-of-steps-to-make-two-strings-anagram.js';
// TODO(backfill): replace with real LeetCode cases

describe('1347-minimum-number-of-steps-to-make-two-strings-anagram', () => {
    it('should handle basic case', () => {
        // expect(minimumNumberOfStepsToMakeTwoStringsAnagram(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(minimumNumberOfStepsToMakeTwoStringsAnagram(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(minimumNumberOfStepsToMakeTwoStringsAnagram(...)).toEqual(...);
    });
});
