import { makeNumberOfDistinctCharactersEqual } from '../2531-make-number-of-distinct-characters-equal.js';
// TODO(backfill): replace with real LeetCode cases

describe('2531-make-number-of-distinct-characters-equal', () => {
    it('should handle basic case', () => {
        // expect(makeNumberOfDistinctCharactersEqual(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(makeNumberOfDistinctCharactersEqual(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(makeNumberOfDistinctCharactersEqual(...)).toEqual(...);
    });
});
