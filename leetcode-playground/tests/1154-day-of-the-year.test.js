import { dayOfTheYear } from '../1154-day-of-the-year.js';

describe('1154-day-of-the-year', () => {
    it('should handle basic case', () => {
        // expect(dayOfTheYear(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(dayOfTheYear(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(dayOfTheYear(...)).toEqual(...);
    });
});
