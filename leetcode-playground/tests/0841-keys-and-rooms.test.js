import { keysAndRooms } from '../0841-keys-and-rooms.js';

describe('0841-keys-and-rooms', () => {
    it('should handle basic case', () => {
        // expect(keysAndRooms(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(keysAndRooms(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(keysAndRooms(...)).toEqual(...);
    });
});
