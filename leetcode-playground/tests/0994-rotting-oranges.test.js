import { rottingOranges } from '../0994-rotting-oranges.js';

describe('0994-rotting-oranges', () => {
    it('should handle basic case', () => {
        // expect(rottingOranges(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(rottingOranges(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(rottingOranges(...)).toEqual(...);
    });
});
