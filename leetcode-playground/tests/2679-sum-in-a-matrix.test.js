import { sumInAMatrix } from '../2679-sum-in-a-matrix.js';

describe('2679-sum-in-a-matrix', () => {
    it('should handle basic case', () => {
        // expect(sumInAMatrix(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(sumInAMatrix(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(sumInAMatrix(...)).toEqual(...);
    });
});
