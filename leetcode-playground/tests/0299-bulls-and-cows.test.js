import { bullsAndCows } from '../0299-bulls-and-cows.js';

describe('0299-bulls-and-cows', () => {
    it('should handle basic case', () => {
        // expect(bullsAndCows(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(bullsAndCows(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(bullsAndCows(...)).toEqual(...);
    });
});
