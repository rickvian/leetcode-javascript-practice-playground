import { minimumDeletionCostToMakeAllCharactersEqual } from '../3784-minimum-deletion-cost-to-make-all-characters-equal.js';
// TODO(backfill): replace with real LeetCode cases

describe('3784-minimum-deletion-cost-to-make-all-characters-equal', () => {
    it('should handle basic case', () => {
        // expect(minimumDeletionCostToMakeAllCharactersEqual(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(minimumDeletionCostToMakeAllCharactersEqual(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(minimumDeletionCostToMakeAllCharactersEqual(...)).toEqual(...);
    });
});
