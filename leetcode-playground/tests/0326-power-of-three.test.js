import { powerOfThree } from '../0326-power-of-three.js';

describe('0326-power-of-three', () => {
    it('should handle basic case', () => {
        // expect(powerOfThree(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(powerOfThree(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(powerOfThree(...)).toEqual(...);
    });
});
