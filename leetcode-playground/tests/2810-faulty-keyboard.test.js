import { faultyKeyboard } from '../2810-faulty-keyboard.js';

describe('2810-faulty-keyboard', () => {
    it('should handle basic case', () => {
        // expect(faultyKeyboard(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(faultyKeyboard(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(faultyKeyboard(...)).toEqual(...);
    });
});
