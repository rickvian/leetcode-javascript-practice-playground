import { replaceQuestionMarksInStringToMinimizeItsValue } from '../3081-replace-question-marks-in-string-to-minimize-its-value.js';

describe('3081-replace-question-marks-in-string-to-minimize-its-value', () => {
    it('should handle basic case', () => {
        // expect(replaceQuestionMarksInStringToMinimizeItsValue(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(replaceQuestionMarksInStringToMinimizeItsValue(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(replaceQuestionMarksInStringToMinimizeItsValue(...)).toEqual(...);
    });
});
