import { findPivotIndex } from '../0724-find-pivot-index.js';

describe('0724-find-pivot-index', () => {
    it('should handle basic case', () => {
        // expect(findPivotIndex(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(findPivotIndex(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(findPivotIndex(...)).toEqual(...);
    });
});
