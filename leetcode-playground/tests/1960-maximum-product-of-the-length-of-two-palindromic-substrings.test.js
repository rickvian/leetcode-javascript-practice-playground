import { maximumProductOfTheLengthOfTwoPalindromicSubstrings } from '../1960-maximum-product-of-the-length-of-two-palindromic-substrings.js';
// TODO(backfill): replace with real LeetCode cases

describe('1960-maximum-product-of-the-length-of-two-palindromic-substrings', () => {
    it('should handle basic case', () => {
        // expect(maximumProductOfTheLengthOfTwoPalindromicSubstrings(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(maximumProductOfTheLengthOfTwoPalindromicSubstrings(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(maximumProductOfTheLengthOfTwoPalindromicSubstrings(...)).toEqual(...);
    });
});
