import { convertSortedListToBinarySearchTree } from '../0109-convert-sorted-list-to-binary-search-tree.js';
// TODO(backfill): replace with real LeetCode cases

describe('0109-convert-sorted-list-to-binary-search-tree', () => {
    it('should handle basic case', () => {
        // expect(convertSortedListToBinarySearchTree(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(convertSortedListToBinarySearchTree(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(convertSortedListToBinarySearchTree(...)).toEqual(...);
    });
});
