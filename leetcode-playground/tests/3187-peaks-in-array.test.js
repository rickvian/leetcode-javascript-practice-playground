import { peaksInArray } from '../3187-peaks-in-array.js';

describe('3187-peaks-in-array', () => {
    it('should handle basic case', () => {
        // expect(peaksInArray(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(peaksInArray(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(peaksInArray(...)).toEqual(...);
    });
});
