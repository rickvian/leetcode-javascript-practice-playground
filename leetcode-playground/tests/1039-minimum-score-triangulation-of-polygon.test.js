import { minimumScoreTriangulationOfPolygon } from '../1039-minimum-score-triangulation-of-polygon.js';

describe('1039-minimum-score-triangulation-of-polygon', () => {
    it('should handle basic case', () => {
        // expect(minimumScoreTriangulationOfPolygon(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(minimumScoreTriangulationOfPolygon(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(minimumScoreTriangulationOfPolygon(...)).toEqual(...);
    });
});
