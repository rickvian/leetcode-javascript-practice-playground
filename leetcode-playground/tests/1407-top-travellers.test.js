import { topTravellers } from '../1407-top-travellers.js';

describe('1407-top-travellers', () => {
    it('should handle basic case', () => {
        // expect(topTravellers(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(topTravellers(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(topTravellers(...)).toEqual(...);
    });
});
