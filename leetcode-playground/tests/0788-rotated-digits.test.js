import { rotatedDigits } from '../0788-rotated-digits.js';

describe('0788-rotated-digits', () => {
    it('should handle basic case', () => {
        // expect(rotatedDigits(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(rotatedDigits(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(rotatedDigits(...)).toEqual(...);
    });
});
