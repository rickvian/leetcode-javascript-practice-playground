import { jumpGameV } from '../1340-jump-game-v.js';

describe('1340-jump-game-v', () => {
    it('should handle basic case', () => {
        // expect(jumpGameV(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(jumpGameV(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(jumpGameV(...)).toEqual(...);
    });
});
