import { minimumNumberOfFlipsToMakeTheBinaryStringAlternating } from '../1888-minimum-number-of-flips-to-make-the-binary-string-alternating.js';
// TODO(backfill): replace with real LeetCode cases

describe('1888-minimum-number-of-flips-to-make-the-binary-string-alternating', () => {
    it('should handle basic case', () => {
        // expect(minimumNumberOfFlipsToMakeTheBinaryStringAlternating(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(minimumNumberOfFlipsToMakeTheBinaryStringAlternating(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(minimumNumberOfFlipsToMakeTheBinaryStringAlternating(...)).toEqual(...);
    });
});
