import { arrayNesting } from '../0565-array-nesting.js';

describe('0565-array-nesting', () => {
    it('should handle basic case', () => {
        // expect(arrayNesting(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(arrayNesting(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(arrayNesting(...)).toEqual(...);
    });
});
