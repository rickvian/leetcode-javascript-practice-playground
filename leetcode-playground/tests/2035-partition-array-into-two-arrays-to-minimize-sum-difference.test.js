import { partitionArrayIntoTwoArraysToMinimizeSumDifference } from '../2035-partition-array-into-two-arrays-to-minimize-sum-difference.js';
// TODO(backfill): replace with real LeetCode cases

describe('2035-partition-array-into-two-arrays-to-minimize-sum-difference', () => {
    it('should handle basic case', () => {
        // expect(partitionArrayIntoTwoArraysToMinimizeSumDifference(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(partitionArrayIntoTwoArraysToMinimizeSumDifference(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(partitionArrayIntoTwoArraysToMinimizeSumDifference(...)).toEqual(...);
    });
});
