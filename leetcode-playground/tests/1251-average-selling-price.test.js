import { averageSellingPrice } from '../1251-average-selling-price.js';

describe('1251-average-selling-price', () => {
    it('should handle basic case', () => {
        // expect(averageSellingPrice(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(averageSellingPrice(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(averageSellingPrice(...)).toEqual(...);
    });
});
