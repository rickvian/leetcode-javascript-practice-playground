import { redistributeCharactersToMakeAllStringsEqual } from '../1897-redistribute-characters-to-make-all-strings-equal.js';
// TODO(backfill): replace with real LeetCode cases

describe('1897-redistribute-characters-to-make-all-strings-equal', () => {
    it('should handle basic case', () => {
        // expect(redistributeCharactersToMakeAllStringsEqual(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(redistributeCharactersToMakeAllStringsEqual(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(redistributeCharactersToMakeAllStringsEqual(...)).toEqual(...);
    });
});
