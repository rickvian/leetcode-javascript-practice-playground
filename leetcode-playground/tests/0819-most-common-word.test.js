import { mostCommonWord } from '../0819-most-common-word.js';

describe('0819-most-common-word', () => {
    it('should handle basic case', () => {
        // expect(mostCommonWord(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(mostCommonWord(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(mostCommonWord(...)).toEqual(...);
    });
});
