import { monthlyTransactionsI } from '../1193-monthly-transactions-i.js';

describe('1193-monthly-transactions-i', () => {
    it('should handle basic case', () => {
        // expect(monthlyTransactionsI(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(monthlyTransactionsI(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(monthlyTransactionsI(...)).toEqual(...);
    });
});
