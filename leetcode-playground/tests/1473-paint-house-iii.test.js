import { paintHouseIii } from '../1473-paint-house-iii.js';

describe('1473-paint-house-iii', () => {
    it('should handle basic case', () => {
        // expect(paintHouseIii(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(paintHouseIii(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(paintHouseIii(...)).toEqual(...);
    });
});
