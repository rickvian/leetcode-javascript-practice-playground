import { numberOfRestrictedPathsFromFirstToLastNode } from '../1786-number-of-restricted-paths-from-first-to-last-node.js';

describe('1786-number-of-restricted-paths-from-first-to-last-node', () => {
    it('should handle basic case', () => {
        // expect(numberOfRestrictedPathsFromFirstToLastNode(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(numberOfRestrictedPathsFromFirstToLastNode(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(numberOfRestrictedPathsFromFirstToLastNode(...)).toEqual(...);
    });
});
