import { equalSubstring } from '../1208-get-equal-substrings-within-budget.js';

describe('1208-get-equal-substrings-within-budget', () => {
  it("equalSubstring([])", () => {
    const result = equalSubstring([]);
    expect(result).toEqual(0);
  });
});
