import { rleIterator } from '../0900-rle-iterator.js';

describe('0900-rle-iterator', () => {
    it('should handle basic case', () => {
        // expect(rleIterator(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(rleIterator(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(rleIterator(...)).toEqual(...);
    });
});
