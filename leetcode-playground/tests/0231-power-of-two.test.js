import { powerOfTwo } from '../0231-power-of-two.js';

describe('0231-power-of-two', () => {
    it('should handle basic case', () => {
        // expect(powerOfTwo(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(powerOfTwo(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(powerOfTwo(...)).toEqual(...);
    });
});
