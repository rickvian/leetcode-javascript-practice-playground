import { jobScheduling } from '../1235-maximum-profit-in-job-scheduling.js';

describe('1235-maximum-profit-in-job-scheduling', () => {
  it("jobScheduling([])", () => {
    const result = jobScheduling([]);
    expect(result).toEqual(0);
  });
});
