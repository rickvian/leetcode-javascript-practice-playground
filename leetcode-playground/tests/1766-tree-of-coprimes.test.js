import { treeOfCoprimes } from '../1766-tree-of-coprimes.js';

describe('1766-tree-of-coprimes', () => {
    it('should handle basic case', () => {
        // expect(treeOfCoprimes(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(treeOfCoprimes(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(treeOfCoprimes(...)).toEqual(...);
    });
});
