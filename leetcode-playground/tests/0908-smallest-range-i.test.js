import { smallestRangeI } from '../0908-smallest-range-i.js';

describe('0908-smallest-range-i', () => {
    it('should handle basic case', () => {
        // expect(smallestRangeI(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(smallestRangeI(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(smallestRangeI(...)).toEqual(...);
    });
});
