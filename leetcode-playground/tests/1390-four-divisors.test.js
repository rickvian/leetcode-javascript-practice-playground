import { fourDivisors } from '../1390-four-divisors.js';

describe('1390-four-divisors', () => {
    it('should handle basic case', () => {
        // expect(fourDivisors(...)).toEqual(...);
    });

    it('should handle empty / minimum input', () => {
        // expect(fourDivisors(...)).toEqual(...);
    });

    it('should handle edge case', () => {
        // expect(fourDivisors(...)).toEqual(...);
    });
});
