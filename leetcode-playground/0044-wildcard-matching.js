/**
 * 44. Wildcard Matching https://leetcode.com/problems/wildcard-matching/ Difficulty: Hard Given an input string (s) and a pattern (p), implement wildcard pattern matching with support for '?' and '*' where: - '?' Matches any single character.
 *
 * https://leetcode.com/problems/wildcard-matching/description/
 *
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function(s, p) {
    // implement
};

export { isMatch };
