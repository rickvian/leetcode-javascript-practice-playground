/**
 * Implement wildcard pattern matching where '?' matches any single character and '*' matches any sequence of characters (including empty).
 *
 * https://leetcode.com/problems/wildcard-matching/description/
 *
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function(s, p) {
    // implement
};

export { isMatch }
