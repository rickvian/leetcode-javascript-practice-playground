/**
 * SQL problem: return employees with the highest salary within each department
 * (ties included). Output columns: Department, Employee, Salary.
 *
 * https://leetcode.com/problems/department-highest-salary/description/
 *
 * @param {Array<{id:number,name:string,salary:number,departmentId:number}>} employees
 * @param {Array<{id:number,name:string}>} departments
 * @return {Array<{Department:string,Employee:string,Salary:number}>}
 */
var departmentHighestSalary = function(employees, departments) {
    // implement
};

export { departmentHighestSalary }
