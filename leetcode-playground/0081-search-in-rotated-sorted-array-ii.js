/**
 * Given an integer array nums sorted in non-decreasing order (possibly rotated and with
 * duplicates) and a target, return true if target is in nums, or false otherwise.
 *
 * https://leetcode.com/problems/search-in-rotated-sorted-array-ii/description/
 *
 * @param {number[]} nums
 * @param {number} target
 * @return {boolean}
 */
var search = function(nums, target) {
    // implement
};

export { search }
