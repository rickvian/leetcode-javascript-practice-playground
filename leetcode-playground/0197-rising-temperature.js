/**
 * Find the ids of dates that have higher temperatures compared to the
 * previous day; modeled as a JS function over an array of weather records.
 *
 * https://leetcode.com/problems/rising-temperature/description/
 *
 * @param {Array<{id: number, recordDate: string, temperature: number}>} weather - Weather records
 * @return {number[]} Ids of dates whose temperature is higher than the immediately previous calendar day
 */
var risingTemperature = function(weather) {
    // implement
};

export { risingTemperature }
