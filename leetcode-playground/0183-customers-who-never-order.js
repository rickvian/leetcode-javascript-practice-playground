/**
 * SQL problem: return customers who never placed an order.
 *
 * https://leetcode.com/problems/customers-who-never-order/description/
 *
 * @param {Array<{id:number,name:string}>} customers
 * @param {Array<{id:number,customerId:number}>} orders
 * @return {Array<{Customers:string}>}
 */
var customersWhoNeverOrder = function(customers, orders) {
    // implement
};

export { customersWhoNeverOrder }
