/**
 * 942. DI String Match https://leetcode.com/problems/di-string-match/ Difficulty: Easy A permutation perm of n + 1 integers of all the integers in the range [0, n] can be represented as a string s of length n where: - s[i] == 'I' if perm[i] < perm[i + 1], and - s[i] == 'D' if perm[i] > perm[i + 1].
 *
 * https://leetcode.com/problems/di-string-match/description/
 *
 * @param {string} s
 * @return {number[]}
 */
var diStringMatch = function(s) {
    // implement
};

export { diStringMatch };
