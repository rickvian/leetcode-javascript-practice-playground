/**
 * 368. Largest Divisible Subset https://leetcode.com/problems/largest-divisible-subset/ Difficulty: Medium Given a set of distinct positive integers nums, return the largest subset answer such that every pair (answer[i], answer[j]) of elements in this subset satisfies: - answer[i] % answer[j] == 0, or - answer[j] % answer[i] == 0 If there are multiple solutions, return any of them.
 *
 * https://leetcode.com/problems/largest-divisible-subset/description/
 *
 * @param {number[]} nums
 * @return {number[]}
 */
var largestDivisibleSubset = function(nums) {
    // implement
};

export { largestDivisibleSubset };
