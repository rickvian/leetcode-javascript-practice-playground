/**
 * 282. Expression Add Operators https://leetcode.com/problems/expression-add-operators/ Difficulty: Hard Given a string num that contains only digits and an integer target, return all possibilities to insert the binary operators '+', '-', and/or '*' between the digits of num so that the resultant expression evaluates to the target value.
 *
 * https://leetcode.com/problems/expression-add-operators/description/
 *
 * @param {string} num
 * @param {number} target
 * @return {string[]}
 */
var addOperators = function(num, target) {
    // implement
};

export { addOperators };
