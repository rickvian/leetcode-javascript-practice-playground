/**
 * Given a pattern and a string s, find if s follows the same pattern (full word bijection).
 *
 * https://leetcode.com/problems/word-pattern/description/
 *
 * @param {string} pattern
 * @param {string} s
 * @return {boolean}
 */
var wordPattern = function(pattern, s) {
    // implement
};

export { wordPattern }
