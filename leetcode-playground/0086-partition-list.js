/**
 * 86. Partition List https://leetcode.com/problems/partition-list/ Difficulty: Medium Given the head of a linked list and a value x, partition it such that all nodes less than x come before nodes greater than or equal to x.
 *
 * https://leetcode.com/problems/partition-list/description/
 *
 * @param {ListNode} head
 * @param {number} x
 * @return {ListNode}
 */
var partition = function(head, x) {
    // implement
};

export { partition };
