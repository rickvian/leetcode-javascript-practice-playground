/**
 * 854. K-Similar Strings https://leetcode.com/problems/k-similar-strings/ Difficulty: Hard Strings s1 and s2 are k-similar (for some non-negative integer k) if we can swap the positions of two letters in s1 exactly k times so that the resulting string equals s2.
 *
 * https://leetcode.com/problems/k-similar-strings/description/
 *
 * @param {string} s1
 * @param {string} s2
 * @return {number}
 */
var kSimilarity = function(s1, s2) {
    // implement
};

export { kSimilarity };
