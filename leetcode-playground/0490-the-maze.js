/**
 * 490. The Maze https://leetcode.com/problems/the-maze/ Difficulty: Medium There is a ball in a maze with empty spaces (represented as 0) and walls (represented as 1).
 *
 * https://leetcode.com/problems/the-maze/description/
 *
 * @param {number[][]} maze
 * @param {number[]} start
 * @param {number[]} destination
 * @return {boolean}
 */
var hasPath = function(maze, start, destination) {
    // implement
};

export { hasPath };
