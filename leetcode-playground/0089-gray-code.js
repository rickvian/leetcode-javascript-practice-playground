/**
 * Return any valid n-bit gray code sequence. The sequence must start with 0 and every adjacent
 * pair (including the last-first pair) must differ in exactly one bit.
 *
 * https://leetcode.com/problems/gray-code/description/
 *
 * @param {number} n
 * @return {number[]}
 */
var grayCode = function(n) {
    // implement
};

export { grayCode }
