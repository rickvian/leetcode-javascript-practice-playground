/**
 * Given a sorted unique integer array and an inclusive range [lower, upper], return
 * the sorted list of ranges missing from nums within [lower, upper].
 *
 * https://leetcode.com/problems/missing-ranges/description/
 *
 * @param {number[]} nums
 * @param {number} lower
 * @param {number} upper
 * @return {number[][]}
 */
var findMissingRanges = function(nums, lower, upper) {
    // implement
};

export { findMissingRanges }
