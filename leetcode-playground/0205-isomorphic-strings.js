/**
 * Given two strings s and t, determine if they are isomorphic—each character
 * in s can be replaced to form t with a consistent one-to-one mapping.
 *
 * https://leetcode.com/problems/isomorphic-strings/description/
 *
 * @param {string} s - First string
 * @param {string} t - Second string
 * @return {boolean} True if s and t are isomorphic
 */
var isIsomorphic = function(s, t) {
    // implement
};

export { isIsomorphic }
