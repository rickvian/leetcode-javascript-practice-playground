/**
 * 1169. Invalid Transactions https://leetcode.com/problems/invalid-transactions/ Difficulty: Medium A transaction is possibly invalid if: - the amount exceeds $1000, or; - if it occurs within (and including) 60 minutes of another transaction with the same name in a different city.
 *
 * https://leetcode.com/problems/invalid-transactions/description/
 *
 * @param {string[]} transactions
 * @return {string[]}
 */
var invalidTransactions = function(transactions) {
    // implement
};

export { invalidTransactions };
