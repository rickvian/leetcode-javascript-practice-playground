/**
 * 776. Split BST https://leetcode.com/problems/split-bst/ Difficulty: Medium Given the root of a binary search tree (BST) and an integer target, split the tree into two subtrees where the first subtree has nodes that are all smaller or equal to the target value, while the second subtree has all nodes that are greater than the target value.
 *
 * https://leetcode.com/problems/split-bst/description/
 *
 * @param {TreeNode} root
 * @param {number} target
 * @return {TreeNode[]}
 */
var splitBST = function(root, target) {
    // implement
};

export { splitBST };
