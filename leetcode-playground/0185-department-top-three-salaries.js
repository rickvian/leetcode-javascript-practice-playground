/**
 * SQL problem: within each department return employees whose salary is among the top three
 * distinct salaries for that department (ties included).
 *
 * https://leetcode.com/problems/department-top-three-salaries/description/
 *
 * @param {Array<{id:number,name:string,salary:number,departmentId:number}>} employees
 * @param {Array<{id:number,name:string}>} departments
 * @return {Array<{Department:string,Employee:string,Salary:number}>}
 */
var departmentTopThreeSalaries = function(employees, departments) {
    // implement
};

export { departmentTopThreeSalaries }
