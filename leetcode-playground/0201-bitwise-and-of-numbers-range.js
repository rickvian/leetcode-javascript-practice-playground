/**
 * Given two integers left and right, return the bitwise AND of all numbers in
 * the inclusive range [left, right].
 *
 * https://leetcode.com/problems/bitwise-and-of-numbers-range/description/
 *
 * @param {number} left - Lower bound of the inclusive range
 * @param {number} right - Upper bound of the inclusive range
 * @return {number} Bitwise AND of every integer from left through right
 */
var rangeBitwiseAnd = function(left, right) {
    // implement
};

export { rangeBitwiseAnd }
