/**
 * Return the knight's minimum initial health required to reach the bottom-right
 * room of the dungeon while never dropping to 0 or below.
 *
 * https://leetcode.com/problems/dungeon-game/description/
 *
 * @param {number[][]} dungeon
 * @return {number}
 */
var calculateMinimumHP = function(dungeon) {
    // implement
};

export { calculateMinimumHP }
