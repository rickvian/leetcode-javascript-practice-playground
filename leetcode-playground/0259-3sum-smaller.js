/**
 * 259. 3Sum Smaller https://leetcode.com/problems/3sum-smaller/ Difficulty: Medium Given an array of n integers nums and an integer target, find the number of index triplets i, j, k with 0 <= i < j < k < n that satisfy the condition nums[i] + nums[j] + nums[k] < target.
 *
 * https://leetcode.com/problems/3sum-smaller/description/
 *
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var threeSumSmaller = function(nums, target) {
    // implement
};

export { threeSumSmaller };
