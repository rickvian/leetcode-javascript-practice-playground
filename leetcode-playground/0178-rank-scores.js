/**
 * SQL problem: rank scores with dense ranking (ties share rank, no gaps),
 * return rows sorted by score descending.
 *
 * https://leetcode.com/problems/rank-scores/description/
 *
 * @param {Array<{id:number,score:number}>} scores
 * @return {Array<{score:number,rank:number}>}
 */
var rankScores = function(scores) {
    // implement
};

export { rankScores }
