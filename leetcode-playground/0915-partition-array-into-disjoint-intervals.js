/**
 * 915. Partition Array into Disjoint Intervals https://leetcode.com/problems/partition-array-into-disjoint-intervals/ Difficulty: Medium Given an integer array nums, partition it into two (contiguous) subarrays left and right so that: - Every element in left is less than or equal to every element in right.
 *
 * https://leetcode.com/problems/partition-array-into-disjoint-intervals/description/
 *
 * @param {number[]} nums
 * @return {number}
 */
var partitionDisjoint = function(nums) {
    // implement
};

export { partitionDisjoint };
