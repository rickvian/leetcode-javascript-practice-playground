/**
 * 631. Design Excel Sum Formula https://leetcode.com/problems/design-excel-sum-formula/ Difficulty: Hard Design the basic function of Excel and implement the function of the sum formula.
 *
 * https://leetcode.com/problems/design-excel-sum-formula/description/
 */
var Excel = function() {
    // implement
};

Excel.prototype.set = function(row, column, val) {
    // implement
};

Excel.prototype.get = function(row, column) {
    // implement
};

Excel.prototype.sum = function(row, column, numbers) {
    // implement
};

Excel.prototype.calculateSum = function(cells) {
    // implement
};

Excel.prototype.updateDependents = function(row, column) {
    // implement
};

export { Excel };
