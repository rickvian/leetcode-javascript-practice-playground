/**
 * 780. Reaching Points https://leetcode.com/problems/reaching-points/ Difficulty: Hard Given four integers sx, sy, tx, and ty, return true if it is possible to convert the point (sx, sy) to the point (tx, ty) through some operations, or false otherwise.
 *
 * https://leetcode.com/problems/reaching-points/description/
 *
 * @param {number} sx
 * @param {number} sy
 * @param {number} tx
 * @param {number} ty
 * @return {boolean}
 */
var reachingPoints = function(sx, sy, tx, ty) {
    // implement
};

export { reachingPoints };
